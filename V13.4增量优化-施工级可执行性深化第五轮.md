# 《V13 融合定稿》V13.4 增量优化 — 施工级可执行性深化（第五轮）

**评审日期**：2026-09-11
**评审对象**：《V13 融合定稿》+《十一专家深化评审与优化建议》v2 +《V13.1 增量优化（第二轮）》+《V13.2 增量优化（第三轮）》+《V13.3 增量优化（第四轮·开工前终检）》
**本轮定位**：**施工级可执行性深化。** V13.3 已达到"可开工"状态（总均 4.66，4 处硬伤修复，7 项待拍板全部裁决），但"可开工"不等于"每一步都知道怎么做"。本轮聚焦于把方案从"设计完美"推进到"施工零歧义"——每个动作都有明确的输入、输出、验证标准和失败回退。事件溯源骨架、INIT 种子流水、群内文字指令主路径、Coze L5 第三顺位、11 张表口径、跨日升级门槛、冷归档降权不删、路径单活原则、event_id=message_id、回执三态、权限三档、批量指令、日志四合一、资产三条硬上限、9 字段分类表、4 列流水扩展、安全模式 B 档、错因后甲案、知识关联甲案 —— 前五轮已全部定稿或给出方案，本轮一律采纳为前提。
**本轮聚焦**：12 个"施工到某一步时会发现不知道怎么做"的四阶坑 ——
1. **D1a 权限档位判定标准缺失**（说了"查三档"，但没说"什么情况算哪一档"）
2. **Seed 卡片 ID 映射未定义**（23 张存量卡的 ID 从哪来、格式是什么）
3. **derive 即时触发机制未精确**（说了"即时触发"，但触发是基于新流水写入还是定时轮询）
4. **早报/晚报推送时间可配置性缺失**（说了"08:00 推送"，但用户作息不同怎么办）
5. **新卡上限"日"定义未明确**（自然日还是 24h 滚动窗口？某天没学能补吗）
6. **错因四类判定标准未给出示例**（用户怎么区分"记不清"和"理解错"）
7. **tags 标签命名规范缺失**（"飞书"和"Feishu"算不算同一个标签）
8. **untrusted 转为可信操作流程未定义**（用户怎么知道哪些记录不可信、怎么批量转可信）
9. **告警升级阈值未按类型量化**（说了"连续 3h 升级"，但不同告警类型应该有不同阈值）
10. **每日 3 张选题优先级算法未明确**（10 张卡到期时选哪 3 张）
11. **掌握度 M 五锚点公式具体数值缺失**（说了"五锚点公式"，但锚点值和计算公式没给）
12. **冷归档"降权"具体操作未定义**（说了"降权不删"，但降权是 M×0.5 还是间隔延长还是移出队列）

**评分口径**：1–5 分，分越高越好；**维护简易性分高 = 维护越简单**（沿用）
**核验纪律**：本轮所有算术结论均由脚本复核，见附录 B。主观评分明确标注为**条件性预期**，不冒充实测。

---

## 📌 本轮结论速览

| 项 | 结论 |
|---|---|
| V13.3 矩阵算术 | ✅ **自洽**（求和 358.5，三口径均 4.6558→4.66，脚本复核通过） |
| V13.3 文档一致性 | ✅ **11/11 一致**（V13.3 已预先做同源检查，本轮复核通过） |
| 本轮四阶坑发现 | **12 个**（🔴 P0×3 / 🟠 P1×6 / 🟡 P2×3） |
| V13.4 预期就绪度 | **4.69**（增量项落地后）→ **4.71**（待验证项通过后） |
| 本轮提升 Δ | V13.3 4.66 → V13.4 4.69 = **+0.03**（施工级细节深化的边际收益） |

> **一句话**：V13.3 把施工图的硬伤修掉了，但照着施工图施工时，仍有 12 处"做到这一步不知道具体怎么操作"的歧义——权限档位怎么判定、卡片 ID 怎么生成、derive 怎么触发、选题优先级怎么算、M 值公式是什么。本轮把这 12 处歧义全部补上明确的操作指引、输入输出和验证标准，让方案从"可开工"推进到"施工零歧义"。

---

## 第〇章 本轮 12 个四阶坑（按严重度）

### 0.1 四阶坑发现清单

| 级别 | 编号 | 问题 | 归属 | 修复方式 |
|---|---|---|---|---|
| 🔴 P0 | **N31** | **D1a 权限档位判定标准缺失**：V13.3 说"查 im:message 权限档位（三档）"，但没定义"什么情况算哪一档"。AI 施工时打开飞书控制台，看到一堆权限项，不知道怎么归类到"完整/受限/无"三档 | 第十四章 D1a / 运维手册 | 🟡 给出三档判定的具体检查项和判定流程图 |
| 🔴 P0 | **N32** | **Seed 卡片 ID 映射未定义**：23 张存量卡的卡片 ID 从哪来？是手动创建还是自动生成？ID 格式是什么（数字/字符串/UUID）？如果 ID 与学习卡表的主键不一致，Seed 写入会全部失败 | 第十四章 D1bc / 第六章 Seed SOP | 🟡 定义卡片 ID 生成规则、格式、与学习卡表的映射关系 |
| 🔴 P0 | **N41** | **掌握度 M 五锚点公式具体数值缺失**：V13.1 提到"五锚点公式"，V13.3 作为前提采纳，但从未给出具体的锚点值和计算公式。derive 实现 M 值计算时，不知道用什么公式 | 第二章 2.1 字段 #4 / derive 实现 | 🟡 给出五锚点的具体数值、插值公式、边界条件 |
| 🟠 P1 | **N33** | **derive 即时触发机制未精确**：V13.3 说"derive 即时触发 + 09:07 兜底"，但"即时触发"是基于"新流水写入事件"还是"定时轮询流水表"？如果是前者，飞书多维表格没有 webhook，怎么检测新流水？ | 第十四章 D3a / derive 实现 | 🟡 定义即时触发的实现机制（轮询间隔+增量检测） |
| 🟠 P1 | **N40** | **每日 3 张选题优先级算法未明确**：当有 10 张卡到期时，选哪 3 张？是按 M 值最低？还是按间隔最长？还是按 tags 多样性？还是随机？没有算法就无法实现选题逻辑 | 早报选题逻辑 / derive | 🟡 给出选题优先级的排序公式和 tie-breaker 规则 |
| 🟠 P1 | **N36** | **错因四类判定标准未给出示例**：用户怎么区分"记不清"和"理解错"？"题目歧义"和"已过期"的边界在哪里？没有判定示例，用户会随意标记，导致错因数据不可用 | 第七章 7.2 / 使用手册 | 🟡 给出四类错因的判定流程图和具体示例 |
| 🟠 P1 | **N38** | **untrusted 转为可信操作流程未定义**：安全模式 B 档说"修复后可转为可信"，但用户怎么知道哪些记录是 untrusted？怎么批量转为可信？转可信后 derive 自动重算吗？ | 第五章 5.2 / trust_records.py | 🟡 给出 untrusted 记录的查看方式、转可信操作步骤、转可信后的重算触发 |
| 🟠 P1 | **N35** | **新卡上限"日"定义未明确**："1 张/日"的"日"是自然日（00:00-23:59）还是 24h 滚动窗口？如果用户某天没学，第二天能补 2 张吗？没有明确定义，选题逻辑会有歧义 | 新卡上限规则 / 选题逻辑 | 🟡 定义"日"为自然日，不补卡，给出跨日边界的处理规则 |
| 🟠 P1 | **N42** | **冷归档"降权"具体操作未定义**：V13 定稿说"冷归档降权不删"，但"降权"具体是什么操作？是 M 值×0.5？还是间隔×2？还是从选题队列中移除但保留记录？不同操作对系统行为影响巨大 | 冷归档策略 / derive | 🟡 定义降权的具体操作（间隔×3 + 选题优先级降权），给出归档/恢复的触发条件 |
| 🟡 P2 | **N34** | **早报/晚报推送时间可配置性缺失**：V13.3 说"早报 08:00 推送"，但如果用户 08:00 在开会、或者用户是夜猫子习惯 10:00 开始学习呢？推送时间应该可配置 | 早报/晚报推送 / 配置文件 | 🟡 推送时间写入配置文件，给出默认值和修改方法 |
| 🟡 P2 | **N37** | **tags 标签命名规范缺失**：用户打标签时可能用"飞书"和"Feishu"两种写法，导致同标签关联失效。也可能用过于宽泛的标签（如"学习"）或过于具体的标签（如"2026-09-10 学的第 3 张卡"） | 第十一章 / 使用手册 | 🟡 给出标签命名规范（中文优先、2-4 字、领域级粒度）、标签合并工具 |
| 🟡 P2 | **N39** | **告警升级阈值未按类型量化**：V13.2 说"同一告警类型连续 3 小时未恢复则升级"，但 3h 对所有告警类型都一样吗？权限失效 3h = 系统断供 3h，应该 1h 就升级；解析失败 3h 可能只是用户发了几条闲聊，不需要升级 | 第一章 1.2 / 告警模块 | 🟡 按告警类型给出不同的升级阈值（权限失效 1h / 写入失败 2h / 解析失败 6h） |

**状态统计**：🔴 P0 共 3 / 🟠 P1 共 6 / 🟡 P2 共 3。
**与前四轮的关系**：第一轮（V13）抓"设计层硬伤"，第二轮（V13.1）抓"施工图精度"，第三轮（V13.2）抓"施工到第三步的三阶坑"，第四轮（V13.3）抓"施工图本身的硬伤"，**本轮（V13.4）抓"施工时的操作歧义"** —— 前五轮的方案设计没有错，但照着做时，仍有 12 处"做到这一步不知道具体怎么操作"的地方。这些不解决，施工时会反复停下来问"这个怎么做"，拖慢进度甚至导致错误实现。

### 0.2 为什么这一轮是"施工级可执行性深化"而不是"继续挑坑"

前五轮的问题发现路径是：

```
V13：  方案写得对 → 但第一周施工时会踩空（设计硬伤）
V13.1：施工图能拿去施工 → 但施工图自己的数字对不上、工期太乐观（精度）
V13.2：施工图的数字对了 → 但按图施工到第三步时，对策本身会引入三阶问题（落地细节）
V13.3：三阶坑填上了 → 但施工图本身还有裁决没落实、数字打架、阻塞项没清（终检硬伤）
V13.4：终检硬伤修了 → 但照着施工时，仍有 12 处"做到这一步不知道具体怎么操作"（操作歧义）
```

**施工级深化的核心判断**：V13.3 的总均 4.66 已经接近高位，继续挑"设计层新坑"的边际收益在下降（每轮 +0.10 → +0.09 → +0.00，下一轮可能 +0.02 以下）。而现在真正影响施工效率的不是"还有没有新坑"，而是"现有方案的每一步有没有明确的操作指引"——权限档位怎么判定、卡片 ID 怎么生成、derive 怎么触发、选题优先级怎么算、M 值公式是什么。**这些不解决，施工时会反复停下来确认，拖慢进度甚至导致错误实现。**

**本轮的方法论**：对 V13.3 的每一个施工动作，问一个问题——"AI 照做时，知不知道具体怎么操作？输入是什么？输出是什么？怎么验证成功？失败了怎么回退？"12 个四阶坑全部来自这个追问。

---

## 第一章 🔴 N31：D1a 权限档位判定标准

### 1.1 问题陈述

V13.3 第十四章 D1a 说"查 im:message 权限档位（三档）"，V13 定稿定义了权限三档：

```
档位 A（完整）：自建应用有 im:message 读权限 + 群机器人可发送消息
档位 B（受限）：自建应用无 im:message 读权限，但群机器人可发送消息
档位 C（无）：自建应用无 im:message 读权限，且群机器人不可用
```

**但没有定义"怎么判定当前属于哪一档"的具体检查项。** AI 施工时打开飞书开发者后台，看到的是一堆权限项（im:message、im:message:send_as_bot、im:chat、im:chat:readonly 等等），不知道哪些权限项对应"im:message 读权限"，也不知道"群机器人可发送消息"怎么验证。

推演场景：

```
AI 执行 D1a：
  ① 打开飞书开发者后台 → 找到自建应用 → 权限管理页面
  ② 看到权限列表：im:message（已开通）、im:message:send_as_bot（已开通）、
     im:chat（未开通）、im:chat:readonly（已开通）、contact:user.base:readonly（已开通）...
  ③ AI 困惑："im:message 读权限"到底是 im:message 还是 im:chat:readonly？
     "群机器人可发送消息"是 im:message:send_as_bot 还是需要单独配置机器人？
  ④ AI 随便选了一个判定 → 判定错误 → 主路径选择错误 → 后续施工全部跑偏
```

### 1.2 对策：三档判定的具体检查项和判定流程

```
权限档位判定标准（V13.4 新增，写入运维手册"D1a 操作指引"章节）：

【检查项 1】自建应用 im:message 读权限
  操作路径：飞书开发者后台 → 应用详情 → 权限管理 → 搜索"im:message"
  判定标准：
    ✅ 有"获取群组消息"权限（权限标识 im:message，描述含"读取群组中的消息"）→ 读权限具备
    ❌ 无该权限 → 读权限不具备
  注意：im:chat:readonly 是"获取群组信息"（群名称、群成员），不是"获取群组消息"，
        不要混淆。判定读权限只看 im:message。

【检查项 2】群机器人可发送消息
  操作路径：
    ① 飞书开发者后台 → 应用详情 → 应用能力 → 机器人 → 确认"机器人"能力已启用
    ② 群设置 → 群机器人 → 确认该自建应用的机器人已被添加到复习群
    ③ 实测：在群内 @机器人 发一条测试消息，确认机器人能回复
  判定标准：
    ✅ 机器人能力已启用 + 已添加到复习群 + 实测能回复 → 群机器人可发送
    ❌ 任一条件不满足 → 群机器人不可用

【档位判定流程】
  检查项 1（读权限）  检查项 2（群机器人）  →  档位
  ✅                  ✅                  →  A（完整）：主路径 = 自建应用轮询读消息
  ❌                  ✅                  →  B（受限）：主路径 = 用户手动转发消息给机器人
  ❌                  ❌                  →  C（无）：主路径 = 手动在多维表格中录入流水

【档位判定后的行动】
  档位 A：继续 D1bc，主路径用自建应用轮询
  档位 B：继续 D1bc，但主路径改为"用户转发消息给机器人"，D3a 的轮询读取模块改为
         "机器人接收消息"模块；运维手册中写明"用户每次答题后需转发给机器人"
  档位 C：暂停施工，告知用户"需要先开通飞书自建应用或群机器人"，给出开通指引
```

**验证标准**：D1a 完成后，输出一份《权限档位判定报告》，包含：
- 检查项 1 的截图（权限管理页面，im:message 权限状态）
- 检查项 2 的截图（群机器人列表 + 实测回复截图）
- 档位判定结论（A/B/C）
- 对应主路径选择

**失败回退**：如果检查项 1 或 2 无法完成（比如飞书后台界面改版、找不到对应入口），记录当前所见的界面截图和权限列表，暂停 D1a，告知用户"需要人工确认权限状态"，不要自行猜测档位。

**成本**：D1a 增加约 10min（逐项检查 + 截图 + 写报告），但避免了"档位判定错误导致主路径选错"的灾难性后果。

---

## 第二章 🔴 N32：Seed 卡片 ID 映射规则

### 2.1 问题陈述

V13.3 第十四章 D1bc 说"写 23 条 Seed（--dry-run → manifest → --execute）"，第六章 Seed 回滚 SOP 说"流水表 23 条 INIT，card_id 与学习卡表一一对应"。

**但没有定义 23 张存量卡的卡片 ID 从哪来、格式是什么。** 推演场景：

```
AI 执行 D1bc 的 Seed 写入：
  ① 学习卡表已经建好了（9 字段），但表里还没有数据（空表）
  ② Seed 脚本要写 23 条 INIT 流水，每条需要一个 card_id
  ③ AI 困惑：card_id 应该是什么？
     - 是 1, 2, 3, ..., 23（数字递增）？
     - 是 UUID（如 a1b2c3d4-...）？
     - 是飞书多维表格自动生成的 record_id？
     - 是用户手动给每张卡起的名字（如"card_飞书自动化_001"）？
  ④ 如果 AI 选了数字递增 1-23，但学习卡表的主键是飞书自动生成的 record_id，
     那流水表的 card_id 与学习卡表的 record_id 对不上，后续 derive 关联查询全部失败
```

**这不是小问题**：card_id 是流水表和学习卡表之间的关联键。如果 ID 格式不统一、映射关系不明确，整个系统的数据关联会断裂。

### 2.2 对策：卡片 ID 生成规则和映射关系

```
卡片 ID 生成规则（V13.4 新增，写入运维手册"D1bc 操作指引"章节）：

【ID 格式】
  card_id = 飞书多维表格学习卡表的 record_id（飞书自动生成，格式如 recXXXXXX）
  不使用数字递增、不使用 UUID、不使用自定义名称
  理由：record_id 是飞书多维表格的主键，天然唯一，且 API 查询时直接用 record_id
        可以避免"自定义 ID 与 record_id 不一致"的映射问题

【ID 生成流程（D1bc 执行顺序）】
  步骤 1：先在学习卡表中创建 23 张卡片记录（只填"卡片标题"和"tags"，其他字段留空）
         → 飞书自动为每条记录生成 record_id
  步骤 2：导出学习卡表的 record_id 列表（23 个），按卡片标题排序
  步骤 3：将 record_id 列表写入 seed_manifest.json，作为 Seed 写入的 card_id
  步骤 4：运行 seed_migration.py --dry-run → 确认 manifest 中 23 个 card_id 与
         学习卡表的 record_id 一一对应 → --execute
  步骤 5：验证：流水表 23 条 INIT 的 card_id，全部能在学习卡表中找到对应的 record_id

【seed_manifest.json 格式】
  {
    "version": "1.0",
    "created_at": "2026-09-11T20:00:00+08:00",
    "cards": [
      {
        "card_id": "recABC123",
        "title": "飞书自动化次数计量口径",
        "tags": ["飞书", "自动化"],
        "seed_timestamp": "2026-09-01T08:00:00+08:00",
        "source": "SEED_MIGRATION"
      },
      ...（共 23 条）
    ]
  }

【关键约束】
  1. 必须先创建学习卡表记录，再写 Seed 流水（顺序不能反）
  2. card_id 必须是学习卡表的真实 record_id，不能编造
  3. Seed 写入后，学习卡表的 record_id 不可更改（飞书也不允许更改）
  4. 如果后续新增卡片，card_id 同样使用新记录的 record_id
```

**验证标准**：
- 学习卡表有 23 条记录，每条有 record_id
- seed_manifest.json 有 23 个 card_id，全部与学习卡表的 record_id 匹配
- 流水表有 23 条 INIT，card_id 全部与 manifest 匹配
- 随机抽 3 条 INIT，用 card_id 查询学习卡表，能找到对应记录

**失败回退**：
- 如果步骤 1 创建学习卡记录失败 → 删除已创建的记录，重新创建
- 如果步骤 4 dry-run 发现 card_id 不匹配 → 检查 manifest 中的 card_id 是否与学习卡表一致，修正后重新 dry-run
- 如果步骤 5 验证失败 → 按第六章 Seed 回滚 SOP 执行（删除 来源=SEED_MIGRATION 的记录，修正后重写）

**成本**：D1bc 增加约 10min（先创建学习卡记录 + 导出 record_id + 写 manifest），但避免了"card_id 映射错误导致整个系统数据关联断裂"的灾难性后果。

---

## 第三章 🔴 N41：掌握度 M 五锚点公式

### 3.1 问题陈述

V13.1 第十二章 12.4 提到"掌握度 M 按五锚点公式计算"，V13.3 第二章 2.1 字段 #4 说"掌握度 M：按 V13.1 第十二章 12.4 的五锚点公式计算"。

**但 V13.1 和 V13.3 都没有给出五锚点的具体数值和计算公式。** derive 实现 M 值计算时，不知道用什么公式。

推演场景：

```
AI 实现 derive 的 M 值计算逻辑：
  ① 找到 V13.1 第十二章 12.4，看到"五锚点公式"
  ② 翻遍 V13、V13.1、V13.2、V13.3，找不到五锚点的具体数值
  ③ AI 困惑：五锚点是哪五个？是 (0, 0.25, 0.5, 0.75, 1.0) 还是 (0, 0.2, 0.4, 0.6, 0.8)？
     插值是线性的还是非线性的？边界条件怎么处理？
  ④ AI 随便选了一个公式 → M 值计算错误 → 间隔重复算法的输入错误 → 复习节奏全错
```

### 3.2 对策：五锚点公式的具体数值和计算逻辑

```
掌握度 M 五锚点公式（V13.4 新增，写入 derive 实现规范）：

【五锚点定义】
  锚点 1：连续正确次数 = 0  →  M = 0.00（完全未掌握）
  锚点 2：连续正确次数 = 1  →  M = 0.25（初步接触）
  锚点 3：连续正确次数 = 3  →  M = 0.50（基本掌握）
  锚点 4：连续正确次数 = 5  →  M = 0.75（熟练掌握）
  锚点 5：连续正确次数 = 7  →  M = 1.00（完全掌握，可归档候选）

【插值公式（分段线性插值）】
  设 n = 当前连续正确次数（整数，≥ 0）

  if n == 0:   M = 0.00
  if n == 1:   M = 0.25
  if 1 < n < 3:  M = 0.25 + (n - 1) × (0.50 - 0.25) / (3 - 1)
                    = 0.25 + (n - 1) × 0.125
  if n == 3:   M = 0.50
  if 3 < n < 5:  M = 0.50 + (n - 3) × (0.75 - 0.50) / (5 - 3)
                    = 0.50 + (n - 3) × 0.125
  if n == 5:   M = 0.75
  if 5 < n < 7:  M = 0.75 + (n - 5) × (1.00 - 0.75) / (7 - 5)
                    = 0.75 + (n - 5) × 0.125
  if n >= 7:  M = 1.00（封顶，不再增长）

  简化：相邻锚点之间的斜率都是 0.125/次，所以可以统一为：
    M = min(1.00, 0.25 + (n - 1) × 0.125)  当 n >= 1
    M = 0.00  当 n == 0

【M 值的修正因子】
  基础 M 由连续正确次数决定，但以下情况需要修正：

  修正 1：答错（结果=不会）
    → 连续正确次数归零 → M 回到 0.00（按公式重新计算）
    → 但如果该卡之前 M ≥ 0.5，答错后 M 不直接归零，而是降为 0.25
       （理由：曾经掌握过的卡，答错后不应该完全等同于从未学过）
    → 实现：if 历史最高 M ≥ 0.5 and 当前答错: M = 0.25, 连续正确次数 = 0
              else: M = 0.00, 连续正确次数 = 0

  修正 2：模糊（结果=模糊）
    → 连续正确次数不变（不增加也不减少）
    → M 不变
    → 间隔天数 × 0.8（缩短间隔，提前复习）

  修正 3：untrusted 记录（安全模式 B 档）
    → untrusted=TRUE 的记录不计入连续正确次数，不影响 M
    → 仅计入"累计复习次数"展示

  修正 4：superseded 记录（撤回/编辑）
    → superseded=TRUE 的记录完全不计入 M 计算

【M 值与卡片状态的关系】
  M < 0.25  → 状态 = NOT_STARTED（从未答对过，或答错后归零）
  0.25 ≤ M < 0.75  → 状态 = LEARNING（学习中）
  M ≥ 0.75 且 连续正确次数 ≥ 5  → 状态 = MASTERED（已掌握，进入长间隔）
  用户手动归档  → 状态 = ARCHIVED（不进队列）

【M 值与间隔天数的关系】
  间隔天数 = 基础间隔 × M 系数
  基础间隔：第 1 次复习 = 1 天，第 2 次 = 2 天，第 3 次 = 4 天，第 4 次 = 7 天，
           第 5 次 = 14 天，第 6 次及以上 = 30 天（封顶）
  M 系数：M < 0.25 → 0.5（缩短）；0.25 ≤ M < 0.5 → 0.8；0.5 ≤ M < 0.75 → 1.0；
          M ≥ 0.75 → 1.5（延长）
  例：第 3 次复习，M = 0.5 → 间隔 = 4 × 1.0 = 4 天
      第 3 次复习，M = 0.8 → 间隔 = 4 × 1.5 = 6 天
```

**验证标准**（契约测试新增 ⑳）：
- 连续正确 0 次 → M = 0.00
- 连续正确 1 次 → M = 0.25
- 连续正确 3 次 → M = 0.50
- 连续正确 5 次 → M = 0.75
- 连续正确 7 次 → M = 1.00
- 连续正确 10 次 → M = 1.00（封顶）
- 历史最高 M = 0.5，当前答错 → M = 0.25（不完全归零）
- 历史最高 M = 0.25，当前答错 → M = 0.00（归零）
- 模糊 → M 不变，间隔 × 0.8

**成本**：derive 实现 M 值计算约 30 行代码，契约测试 ⑳ 约 10 个测试用例。但这是系统的核心算法，必须明确。

---

## 第四章 🟠 N33：derive 即时触发机制

### 4.1 问题陈述

V13.3 第十四章 D3a 说"review_derive.py + 即时触发 + 09:07 兜底"，V13.1 也提到"derive 即时触发"。

**但"即时触发"的实现机制未精确。** 飞书多维表格没有数据库触发器（trigger），也没有 webhook 回调。那么"即时触发"是怎么实现的？

推演场景：

```
用户 09:00 发「会 1」→ 轮询读取 → 解析 → 写入流水表（09:00:15 COMMITTED）
  → derive 需要"即时"重算卡片 1 的派生值

问题：derive 怎么知道流水表有新记录？
  方案 A：derive 持续运行，每 60s 轮询流水表，检查是否有新记录
    → 这不是"即时"，是"准实时"（延迟 ≤ 60s）
    → 但持续运行需要一个常驻进程，用户的 ThinkPad T480 能扛住吗？
  方案 B：写入流水后，由写入脚本直接调用 derive 重算
    → 这是真正的"即时"（写入后立刻重算）
    → 但写入脚本和 derive 脚本耦合在一起，如果 derive 失败，写入也失败？
  方案 C：用飞书自动化（多维表格的"自动化流程"功能）检测新记录，触发 webhook
    → 飞书多维表格有"当记录新增时"的自动化触发
    → 但自动化流程有次数限制（V13 已降级为监控项），且 webhook 需要一个公网可访问的端点
    → 用户的 ThinkPad T480 没有公网 IP，webhook 无法访问
```

### 4.2 对策：即时触发 = 写入后同步调用 + 定时兜底

```
derive 触发机制（V13.4 新增，写入 derive 实现规范）：

【主触发：写入后同步调用（真正的即时）】
  流程：
    1. 轮询读取脚本检测到新消息 → 解析 → 写入流水表（COMMITTED）
    2. 写入成功后，**立即调用** derive 重算该卡片的派生值
       → 调用方式：subprocess.run(["python", "review_derive.py", "--card-id", card_id])
       → 只重算受影响的卡片（单卡模式），不全量重算
    3. derive 重算完成 → 更新学习卡表的 6 个写入字段
    4. 如果 derive 重算失败 → 记录 ALERT 日志，不影响流水写入（流水已 COMMITTED）
       → 09:07 兜底会全量重算，修正该卡片

  优点：真正的即时（写入后 1-2s 内完成重算）
  缺点：写入脚本和 derive 耦合，但通过 subprocess 解耦，derive 失败不影响写入

【兜底触发：每日 09:07 全量重算】
  流程：
    1. Windows 任务计划程序（Task Scheduler）每日 09:07 触发
    2. 运行 review_derive.py --rebuild-all
    3. 从流水表全量重算所有卡片的派生值
    4. 与当前学习卡表的值对比，如果有差异 → 记录 ALERT 日志（影子对账）
    5. 用重算结果覆盖学习卡表（单一真源 = 流水）

  作用：
    - 修正主触发失败导致的派生值不一致
    - 修正 derive 逻辑变更后的历史数据
    - 影子对账：检测主触发是否有静默失败

【单卡模式 vs 全量模式】
  --card-id <id>：只重算指定卡片（主触发用，耗时 < 1s）
  --rebuild-all：全量重算所有卡片（兜底用，耗时约 10-30s，取决于卡片数量）
  --dry-run：只计算不写入，输出差异（影子对账用）

【失败处理】
  主触发失败（derive 抛异常或返回非 0 退出码）：
    1. 记录 ALERT 日志：「derive 单卡重算失败：card_id=XXX，错误=...」
    2. 不重试（避免无限循环）
    3. 09:07 兜底会全量重算，修正该卡片
    4. 如果连续 3 天主触发都失败 → 告警升级（按 N39 的告警阈值）

  兜底触发失败：
    1. 记录 ALERT 日志：「derive 全量重算失败：错误=...」
    2. 次日 09:07 再次尝试
    3. 如果连续 2 天兜底失败 → 告警升级，人工介入
```

**验证标准**（契约测试新增 ㉑）：
- 写入流水后 5s 内，学习卡表的派生值已更新（主触发生效）
- 手动修改学习卡表的派生值（模拟主触发失败），09:07 兜底后恢复正确
- derive --dry-run 输出与当前值的差异，不写入
- derive 单卡模式只修改指定卡片，不影响其他卡片

**成本**：
- 写入脚本增加 subprocess 调用 derive：5 行代码
- derive 增加 --card-id 参数：10 行代码
- Windows 任务计划程序配置：5min
- 契约测试 ㉑：5 个测试用例

**与 ThinkPad T480 硬件的兼容性**：主触发是写入后同步调用，不需要常驻进程，对系统资源无额外占用。兜底是每日一次的定时任务，耗时 10-30s，对硬件无压力。

---

## 第五章 🟠 N40：每日 3 张选题优先级算法

### 5.1 问题陈述

V13.3 说"每日 3 张"，但没有定义"当有 10 张卡到期时，选哪 3 张"的选题优先级算法。

推演场景：

```
某天早上 08:00，早报选题：
  学习卡表中有 10 张卡的"下次复习日期" ≤ 今天
  其中：
    - 卡片 A：M=0.2，间隔=1天，连续正确=0（昨天答错了）
    - 卡片 B：M=0.5，间隔=4天，连续正确=3
    - 卡片 C：M=0.8，间隔=14天，连续正确=6（快掌握了）
    - 卡片 D：M=0.3，间隔=2天，连续正确=1，tags=["飞书"]
    - 卡片 E：M=0.4，间隔=2天，连续正确=2，tags=["飞书"]
    - 卡片 F：M=0.6，间隔=7天，连续正确=4，tags=["机电"]
    - ...还有 4 张

  问题：选哪 3 张？
    - 按 M 最低选？→ A(0.2), D(0.3), E(0.4)，但 D 和 E 都是"飞书"标签，
      违反知识关联的"标签多样性选题"原则
    - 按间隔最长选？→ C(14天), F(7天), B(4天)，但这些都是掌握度高的卡，
      忽略了刚答错的 A（最需要复习）
    - 随机选？→ 可能选到 3 张同标签的卡，或 3 张都是高掌握度的卡
    - 没有明确算法 → AI 实现时随便选 → 复习效率低下
```

### 5.2 对策：选题优先级排序公式和 tie-breaker 规则

```
每日 3 张选题算法（V13.4 新增，写入早报选题实现规范）：

【选题池】
  候选卡片 = 学习卡表中满足以下条件的卡片：
    1. 状态 ∈ {NOT_STARTED, LEARNING}（MASTERED 和 ARCHIVED 不进选题池）
    2. 下次复习日期 ≤ 今天（到期的卡）
    3. superseded = FALSE（未被撤回）
  如果候选卡片 < 3 张 → 从"未到期但接近到期"的卡片中补充（下次复习日期 ≤ 今天+2）
  如果仍 < 3 张 → 引入新卡（新卡上限 1 张/日，见 N35），或如实推送实际数量

【优先级排序公式】
  对每张候选卡片，计算"复习紧急度分数" S：

    S = W1 × (1 - M) + W2 × (已超期天数 / 基础间隔) + W3 × 标签多样性加分

  其中：
    W1 = 0.5（掌握度权重：M 越低越紧急）
    W2 = 0.3（超期权重：超期越久越紧急）
    W3 = 0.2（标签多样性权重：与已选卡片标签不同的加分）

  已超期天数 = max(0, 今天 - 下次复习日期)
  基础间隔 = 该卡当前的间隔天数（最小为 1，避免除零）

  标签多样性加分：
    初始 = 0
    如果该卡的 tags 与已选卡片的 tags 无交集 → +0.2
    如果有部分交集 → +0.1
    如果完全相同 → +0

【选题流程（贪心算法）】
  步骤 1：计算所有候选卡片的 S（不含标签多样性加分，W3=0）
  步骤 2：选 S 最高的卡片作为第 1 张
  步骤 3：对剩余候选卡片，重新计算 S（含标签多样性加分，W3=0.2，
         与已选卡片对比）
  步骤 4：选 S 最高的卡片作为第 2 张
  步骤 5：重复步骤 3-4，选第 3 张

【tie-breaker 规则（S 相同时）】
  1. 连续正确次数少的优先（更需要巩固）
  2. 上次复习日期更早的优先（更久没复习）
  3. 卡片 ID 字典序更小的优先（确定性，避免随机）

【新卡引入规则（与 N35 联动）】
  如果候选卡片 < 3 张，且今天还没引入新卡（新卡上限 1 张/日）：
    → 从 NOT_STARTED 状态的卡片中选 1 张加入选题池
    → 新卡的 S = 1.0（最高优先级，因为从未学过）
    → 新卡引入后，今天不再引入更多新卡

【输出格式】
  早报消息中，3 张卡片按 S 从高到低排列：
    「📚 今日复习（3 张）：
     1. [卡片 A 标题]（M=0.2，超期 1 天）
     2. [卡片 F 标题]（M=0.6，今日到期）
     3. [卡片 B 标题]（M=0.5，今日到期）」
```

**验证标准**（契约测试新增 ㉒）：
- 10 张候选卡，选出来的 3 张 S 分数最高
- 3 张候选卡中有 2 张同标签，第 3 张优先选不同标签的
- 候选卡只有 2 张，自动引入 1 张新卡（如果今天没引入过）
- 候选卡只有 2 张，今天已引入过新卡 → 只推送 2 张，不强行凑 3 张
- S 相同时，按 tie-breaker 规则选

**成本**：选题逻辑实现约 40 行代码，契约测试 ㉒ 约 8 个测试用例。

**与知识关联甲案的联动**：选题算法中的"标签多样性加分"就是知识关联甲案的"方式 2：同标签卡不连续推送"。同标签间隔微调（方式 1）在 derive 重算时间隔天数时实现，不影响选题优先级。

---

## 第六章 🟠 N36：错因四类判定标准和示例

### 6.1 问题陈述

V13.3 第七章 7.2 定义了四类错因：记不清 / 理解错 / 题目歧义 / 已过期，并给出了处置差异表。

**但没有给出用户怎么判定"我这属于哪一类"的具体标准和示例。** 用户答题时，看到卡片想了半天，答了"不会"，然后需要选错因——但用户可能不知道怎么区分"记不清"和"理解错"。

推演场景：

```
用户复习卡片：
  问："飞书多维表格自动化运行次数的计量口径是什么？"
  答："按流程计还是按动作计需实测确认，1流程含2动作时可能计1次或2次"

用户想了 30 秒，答"不会"，然后需要选错因：
  - 选项 1：记不清
  - 选项 2：理解错
  - 选项 3：题目歧义
  - 选项 4：已过期

用户困惑：
  "我好像之前学过，但具体答案想不起来了——这是记不清还是理解错？"
  "如果我记错了答案（以为按流程计，实际按动作计），这是理解错还是记不清？"
  "如果题目问得不清楚，我理解成了另一个意思，这是题目歧义还是理解错？"
```

### 6.2 对策：四类错因判定流程图和具体示例

```
错因判定标准（V13.4 新增，写入使用手册"错因标记指南"章节）：

【判定流程图】
  答"不会"后，按以下顺序判断：

  问题 1：这张卡的内容是不是已经过时了？（API 变了 / 政策改了 / 你已经不用这个知识了）
    → 是 → 已过期
    → 否 → 继续

  问题 2：你看到题目时，是不是理解成了另一个意思？（题目问 A，你以为问 B）
    → 是 → 题目歧义
    → 否 → 继续

  问题 3：你之前是不是学过这个知识点，并且曾经答对过？（看历史记录：连续正确次数 ≥ 1）
    → 是 → 记不清（学过但忘了）
    → 否 → 理解错（从未真正理解，或理解有误）

【四类错因的具体示例】

  【记不清】
    特征：曾经答对过，现在想不起来具体内容
    示例：
      - 卡片问"间隔重复的基础间隔序列是什么？"，你之前答对过（1,2,4,7,14,30），
        但今天只记得"好像是 1,2,4..."，后面的想不起来了
      - 卡片问"飞书自建应用的权限三档是什么？"，你之前答对过，
        但今天只记得"完整/受限/无"，具体每档的特征想不起来了
    处置：不重写卡片，缩短间隔，增加提取练习

  【理解错】
    特征：你对知识点的理解有误，或者卡片没把知识点讲清楚
    示例：
      - 卡片问"飞书自动化次数怎么算？"，你以为"按流程计，1 流程 = 1 次"，
        但实际是"按动作计，1 流程含 2 动作 = 2 次"——你的理解是错的
      - 卡片问"掌握度 M 的五锚点是什么？"，你以为是"0,0.2,0.4,0.6,0.8"，
        但实际是"0,0.25,0.5,0.75,1.0"——你记错了锚点值
    处置：重写卡片答案（或拆成更细的卡），version+1，旧流水不计入新版本

  【题目歧义】
    特征：题面写得不清楚，导致你理解成了另一个意思，但答案本身是对的
    示例：
      - 卡片问"飞书自动化次数怎么算？"，你以为问的是"Coze 工作流的次数"，
        但实际问的是"飞书多维表格自动化的次数"——题目没说清楚是哪种自动化
      - 卡片问"间隔是多少？"，你以为问的是"基础间隔"，但实际问的是"当前间隔"
        ——题目没说清楚是哪种间隔
    处置：只改题面，不改答案，version+1，旧流水可迁移到新版本

  【已过期】
    特征：卡片内容已经过时，不再适用
    示例：
      - 卡片问"飞书多维表格自动化的免费额度是多少？"，答案写的是"100 次/月"，
        但飞书已经改了政策，现在是"200 次/月"——旧答案过时了
      - 卡片问"Coze 的免费模型有哪些？"，答案写的是"GPT-3.5"，
        但 Coze 已经下线了 GPT-3.5，现在用豆包模型——旧答案过时了
    处置：直接归档，不重写（如果需要，创建一张新卡片记录最新信息）
```

**错因标记的用户操作指引**（写入使用手册）：

```
答错后如何选错因：
  1. 先看答案，判断"我之前是不是理解对了只是忘了"（记不清）
     还是"我之前的理解就是错的"（理解错）
  2. 如果是理解错，再判断"是答案错了"（理解错，需重写答案）
     还是"题目问得不清楚"（题目歧义，只需改题面）
  3. 如果内容已经过时 → 已过期
  4. 实在不确定 → 选"记不清"（最保守的选项，不会触发卡片重写）

错因标记不是必填的：
  如果你不想选错因，可以直接答"不会"，系统默认错因为空
  错因为空的记录按"记不清"处理（不重写卡片，缩短间隔）
```

**验证标准**：
- 使用手册中有判定流程图和 4 类各 2 个示例
- 错因标记的 UI（如果有按钮）按流程图顺序排列选项
- 契约测试：答错+选错因后，系统按对应处置规则执行

**成本**：使用手册增加约 500 字的错因指南，derive 实现错因处置逻辑约 20 行代码（V13.3 已有状态机，本轮补充判定标准）。

---

## 第七章 🟠 N38：untrusted 转为可信操作流程

### 7.1 问题陈述

V13.3 第五章 5.2 安全模式 B 档说"修复后，用户可选择'将 untrusted 记录转为可信'（逐条确认或全量确认）"，并提到"提供一个脚本 trust_records.py --from-date ... --to-date ..."。

**但没有定义完整的操作流程**：用户怎么知道哪些记录是 untrusted？怎么查看 untrusted 记录的详情？转可信后 derive 自动重算吗？转可信可以撤销吗？

推演场景：

```
场景：feishu_sdk.py 被同步工具覆盖了旧版本，hash 不匹配
  → 系统进入安全模式 B 档，当天的 3 条流水标记为 untrusted=TRUE
  → 群内发了告警："⚠ sdk 版本不匹配，今日答题已记录但标记为不可信"

第 2 天，用户修复了 sdk（重新同步了正确版本），hash 匹配了
  → 系统恢复正常，新流水不再标记 untrusted

问题：
  ① 用户怎么知道昨天有 3 条 untrusted 记录？（告警消息可能被刷上去了）
  ② 用户怎么查看这 3 条记录的详情？（哪张卡、答了什么、时间）
  ③ 用户怎么批量转为可信？（trust_records.py 的参数怎么填？）
  ④ 转可信后，derive 自动重算这些卡片吗？（还是需要手动触发？）
  ⑤ 如果用户转可信后发现转错了，能撤销吗？
```

### 7.2 对策：untrusted 记录的完整管理流程

```
untrusted 记录管理流程（V13.4 新增，写入安全模式 B 档实现规范）：

【1. untrusted 记录的查看方式】

  方式 A：群内指令查询
    用户在复习群发「不可信」或「untrusted」
    → 机器人回复最近 7 天的 untrusted 记录列表：
      「📋 最近 7 天不可信记录（共 3 条）：
       1. 2026-09-10 09:15 卡片A 答"会"（sdk hash 不匹配）
       2. 2026-09-10 09:20 卡片B 答"不会"（sdk hash 不匹配）
       3. 2026-09-10 09:25 卡片C 答"模糊"（sdk hash 不匹配）
       回复「转可信 1-3」可将以上记录转为可信」

  方式 B：多维表格直接查看
    打开复习流水表 → 筛选 untrusted = TRUE
    → 可看到所有 untrusted 记录的详情（卡片、答案、时间、不可信原因）

  方式 C：晚报中提示
    如果有未处理的 untrusted 记录，晚报末尾增加一条提示：
      「⚠ 你有 3 条不可信记录待处理，回复「不可信」查看详情，回复「转可信全部」一键转可信」

【2. untrusted 转可信的操作方式】

  方式 A：群内指令（推荐，最便捷）
    「转可信全部」→ 将所有 untrusted=TRUE 的记录转为可信
    「转可信 1-3」→ 将查询结果中的第 1-3 条转为可信
    「转可信 2026-09-10」→ 将指定日期的 untrusted 记录转为可信

  方式 B：脚本命令（适合批量处理）
    python trust_records.py --all                    # 全部转可信
    python trust_records.py --from-date 2026-09-10 --to-date 2026-09-10
    python trust_records.py --card-id recABC123       # 指定卡片的 untrusted 记录转可信
    python trust_records.py --dry-run                  # 只预览不执行

  方式 C：多维表格手动修改
    打开复习流水表 → 找到 untrusted=TRUE 的记录 → 手动改为 FALSE
    → 不推荐（容易漏改，且不会自动触发 derive 重算）

【3. 转可信后的自动处理】

  转可信操作执行后：
    1. 将指定记录的 untrusted 字段改为 FALSE
    2. 记录 ALERT 日志：「untrusted 转可信：3 条记录，操作人=用户，操作方式=群内指令」
    3. **自动触发 derive 重算**受影响的卡片（单卡模式，见 N33）
       → 因为这些记录之前不计入 M/间隔计算，现在转为可信后需要重新计算
    4. 群内回复确认：「✅ 已将 3 条记录转为可信，已触发 3 张卡片的派生值重算」

【4. 转可信的撤销】

  如果用户转可信后发现转错了（比如那些记录确实是不可信的）：
    → 可以在 24 小时内撤销：群内回复「撤销转可信」
    → 系统将最近一次转可信操作的记录重新标记为 untrusted=TRUE
    → 自动触发 derive 重算（恢复到转可信前的状态）
    → 超过 24 小时不可撤销（因为 derive 可能已经基于这些数据做了多次重算，
      撤销会导致数据不一致）

【5. untrusted 记录的自动清理】

  如果 untrusted 记录超过 30 天未处理：
    → 系统自动将其转为可信（理由：30 天了用户都没提出异议，说明数据是可信的）
    → 自动触发 derive 重算
    → 群内发一条通知：「📋 30 天前的 3 条不可信记录已自动转为可信」
  这个自动清理可以在配置中关闭（如果用户希望手动处理所有 untrusted 记录）
```

**验证标准**（契约测试新增 ㉓）：
- 群内发「不可信」→ 机器人回复 untrusted 记录列表
- 群内发「转可信全部」→ 所有 untrusted 记录转为 FALSE，derive 自动重算受影响卡片
- trust_records.py --dry-run → 输出将转可信的记录数，不执行
- 转可信后 24h 内发「撤销转可信」→ 记录恢复为 untrusted=TRUE
- untrusted 记录超过 30 天 → 自动转可信（如果配置开启）

**成本**：
- 群内指令解析（不可信/转可信/撤销转可信）：约 30 行代码
- trust_records.py 脚本：约 50 行代码
- 转可信后自动触发 derive 重算：复用 N33 的单卡模式，5 行代码
- 契约测试 ㉓：6 个测试用例

---

## 第八章 🟠 N35：新卡上限"日"定义

### 8.1 问题陈述

V13.3 裁决③说"新卡上限 1 张/日"，D1bc 建表时写入规则。

**但"日"的定义未明确**：是自然日（00:00-23:59）还是 24h 滚动窗口？如果用户某天没学，第二天能补 2 张吗？

推演场景：

```
场景 1：自然日定义
  第 1 天：用户没学（0 张新卡引入）
  第 2 天：用户学了，选题时候选卡只有 2 张到期，需要引入新卡
    → 按"1 张/日"，今天只能引入 1 张
    → 但昨天没引入，今天能补 2 张吗？
    → 如果不能补，那昨天的"配额"就浪费了
    → 如果能补，那"1 张/日"的上限就没意义了（可以攒到一天引入 7 张）

场景 2：24h 滚动窗口
  用户第 1 天 23:00 引入了 1 张新卡
  第 2 天 00:30 又想引入新卡
    → 按 24h 滚动窗口，距离上次引入只有 1.5h，不能引入
    → 但按自然日，今天是新的一天，可以引入
    → 两种定义的结果完全不同
```

### 8.2 对策：自然日定义，不补卡

```
新卡上限"日"定义（V13.4 新增，写入选题实现规范）：

【定义】
  "日" = 自然日（00:00:00 - 23:59:59，用户本地时区）
  新卡上限 = 每个自然日最多引入 1 张新卡（NOT_STARTED → LEARNING）

【不补卡原则】
  某天没有引入新卡（用户没学 / 候选卡足够 / 系统故障），该日的配额**不累计、不补**
  理由：
    1. 间隔重复的核心是"少量多次"，攒到一天引入多张新卡会导致认知过载
    2. 补卡机制会让"新卡上限"失去约束力（用户可以攒 7 天然后一天引入 7 张）
    3. 不补卡简化了实现（不需要记录"历史配额使用情况"，只需要记录"今天是否引入过"）

【跨日边界处理】
  23:59 引入新卡 → 属于今天的配额
  00:01 引入新卡 → 属于明天的配额
  （自然日边界清晰，不存在 24h 滚动窗口的"边界模糊"问题）

【实现方式】
  学习卡表或系统配置中记录：
    last_new_card_date = "2026-09-11"（最后一次引入新卡的日期，YYYY-MM-DD 格式）

  选题时判断是否可以引入新卡：
    if last_new_card_date == 今天:
      → 今天已引入过新卡，不再引入
    else:
      → 今天还没引入过新卡，可以引入 1 张
      → 引入后更新 last_new_card_date = 今天

  注意：last_new_card_date 只记录"日期"，不记录"时间"，因为是自然日定义

【新卡引入的触发条件】
  只有同时满足以下条件时才引入新卡：
    1. 今天还没引入过新卡（last_new_card_date != 今天）
    2. 选题池中的到期卡片 < 3 张（需要新卡凑数）
    3. 有 NOT_STARTED 状态的卡片可供引入
    4. 用户没有开启"暂停新卡"模式（倦怠检测红色预警时自动暂停）

【新卡引入后的初始化】
  新卡引入时：
    1. 状态从 NOT_STARTED → LEARNING
    2. 连续正确次数 = 0
    3. M = 0.00
    4. 间隔天数 = 1（第一次复习间隔为 1 天）
    5. 上次复习日期 = 今天（首次学习视为一次复习）
    6. 最后结果 = 空（还没答过）
    7. 写入一条 INIT 流水（来源=NEW_CARD，不是 SEED_MIGRATION）
    8. 更新 last_new_card_date = 今天
```

**验证标准**（契约测试新增 ㉔）：
- 今天没引入过新卡，候选卡只有 2 张 → 引入 1 张新卡，last_new_card_date 更新为今天
- 今天已引入过新卡，候选卡只有 2 张 → 不引入新卡，只推送 2 张
- 昨天没引入新卡，今天候选卡只有 1 张 → 只引入 1 张（不补昨天的配额）
- 23:59 引入新卡 → last_new_card_date = 今天
- 00:01 引入新卡 → last_new_card_date = 明天（新的自然日）
- 倦怠检测红色预警时 → 不引入新卡（即使今天没引入过）

**成本**：
- last_new_card_date 记录：可以放在系统事件日志表（log_type=CONFIG）或单独的配置文件，5 行代码
- 选题时的新卡引入判断：10 行代码
- 新卡初始化逻辑：15 行代码
- 契约测试 ㉔：6 个测试用例

---

## 第九章 🟠 N42：冷归档"降权"具体操作

### 9.1 问题陈述

V13 定稿定义了"冷归档降权不删"策略，V13.3 作为前提采纳。但"降权"具体是什么操作从未定义。

推演场景：

```
用户有一张卡片，连续正确 7 次，M=1.0，状态=MASTERED，间隔=30 天
  → 按 V13 的"冷归档"策略，这张卡应该"降权不删"

问题：
  ① "降权"是 M 值从 1.0 降到 0.5？还是间隔从 30 天延长到 90 天？
     还是从选题队列中移除但保留记录？
  ② 降权后，这张卡还会出现在早报中吗？
  ③ 如果用户想恢复这张卡（比如发现自己忘了），怎么恢复？
  ④ 降权是自动的（M≥1.0 持续 N 天后自动降权）还是手动的（用户说"归档 3"）？
```

### 9.2 对策：冷归档降权的具体操作和触发条件

```
冷归档降权策略（V13.4 新增，写入 derive 实现规范和使用手册）：

【"降权"的具体操作】
  冷归档 = 间隔 × 3 + 选题优先级降权，**不修改 M 值，不删除记录**

  具体操作：
    1. 间隔天数 × 3（延长复习间隔，减少出现频率）
       例：间隔 30 天 → 90 天；间隔 14 天 → 42 天
    2. 选题优先级降权：在 N40 的选题公式中，冷归档卡片的 S 分数 × 0.3
       （即使到期了，也优先选非冷归档的卡）
    3. 状态不变（仍为 MASTERED），M 值不变，流水记录不变
    4. 学习卡表增列 `cold_archived`（布尔，默认 FALSE）标记是否冷归档

  不做的操作：
    ❌ 不修改 M 值（M 是掌握度的真实反映，降权不代表掌握度下降）
    ❌ 不删除卡片记录（事件溯源只追加，不删除）
    ❌ 不删除流水记录（历史数据保留）
    ❌ 不从选题池中移除（只是优先级降低，如果其他卡都复习完了，还是会选到）

【冷归档的触发条件】

  自动触发（满足任一条件）：
    1. M ≥ 0.75 且 连续正确次数 ≥ 7 且 最近 3 次复习全部答对
       → 持续掌握，进入冷归档
    2. 间隔天数 ≥ 30 且 最近 2 次复习间隔都 ≥ 30 天且全部答对
       → 长间隔仍能保持，进入冷归档

  手动触发：
    用户在群内发「归档 3」→ 将第 3 张卡片冷归档
    用户在多维表格中手动将 cold_archived 改为 TRUE

  自动触发的检查时机：derive 每次重算时检查（单卡模式或全量模式）

【冷归档卡片的复习行为】
  冷归档卡片到期后：
    1. 进入选题池，但 S 分数 × 0.3（优先级降低）
    2. 如果选题池中有非冷归档的卡片，优先选非冷归档的
    3. 如果选题池中只有冷归档的卡片，或非冷归档的卡片不足 3 张，冷归档卡片入选
    4. 冷归档卡片答对后：间隔继续 × 3（已经是 90 天 → 270 天，封顶 365 天）
    5. 冷归档卡片答错后：**自动恢复**（cold_archived → FALSE），
       M 按答错规则处理（见 N41 修正 1），间隔恢复为基础间隔

【冷归档的恢复】
  自动恢复：
    冷归档卡片答错 → 自动恢复为非冷归档（cold_archived = FALSE）
    理由：答错说明还没完全掌握，需要恢复正常复习节奏

  手动恢复：
    用户在群内发「恢复 3」→ 将第 3 张卡片从冷归档恢复
    用户在多维表格中手动将 cold_archived 改为 FALSE

  恢复后的处理：
    1. cold_archived = FALSE
    2. 间隔天数恢复为冷归档前的间隔（÷3），或按当前 M 值重新计算
    3. 选题优先级恢复正常
    4. M 值不变（恢复不改变掌握度）

【冷归档与 MASTERED 状态的关系】
  MASTERED = 掌握度达标（M ≥ 0.75 且连续正确 ≥ 5），正常长间隔复习
  冷归档 = MASTERED 的进一步降级（间隔 ×3，优先级降权），适用于"完全掌握、不需要频繁复习"的卡

  状态流转：
    NOT_STARTED → LEARNING → MASTERED → 冷归档（可选）
    冷归档 → 答错/手动恢复 → MASTERED（回到正常长间隔）
    任何状态 → 用户手动归档 → ARCHIVED（完全不进队列，与冷归档不同）

【ARCHIVED 与冷归档的区别】
  ARCHIVED：完全不进选题池，不复习，用户手动操作（「跳过 3」或手动改状态）
  冷归档：仍进选题池，但优先级降低，间隔延长，自动或手动触发
  → 冷归档是"减少复习频率"，ARCHIVED 是"停止复习"
```

**验证标准**（契约测试新增 ㉕）：
- M=1.0，连续正确 7 次，最近 3 次全对 → 自动冷归档，间隔 ×3，cold_archived=TRUE
- 冷归档卡片在选题池中 S × 0.3，优先级低于非冷归档卡片
- 冷归档卡片答对 → 间隔继续 ×3（封顶 365 天）
- 冷归档卡片答错 → 自动恢复（cold_archived=FALSE），M 按答错规则处理
- 手动「归档 3」→ 卡片 3 冷归档
- 手动「恢复 3」→ 卡片 3 从冷归档恢复
- ARCHIVED 卡片不进选题池，冷归档卡片仍进选题池（优先级低）

**成本**：
- 学习卡表增列 cold_archived（布尔）：D1bc 建表时加，1min
- derive 冷归档自动触发检查：15 行代码
- 选题公式中冷归档降权：5 行代码
- 群内指令「归档 N」「恢复 N」：15 行代码
- 契约测试 ㉕：7 个测试用例

---

## 第十章 🟡 N34：早报/晚报推送时间可配置性

### 10.1 问题陈述

V13.3 说"早报 08:00 推送"，但推送时间是硬编码的还是可配置的？如果用户 08:00 在开会、或者用户是夜猫子习惯 10:00 开始学习呢？

### 10.2 对策：推送时间写入配置文件

```
推送时间配置（V13.4 新增，写入配置文件 config.yaml）：

【配置项】
  schedule:
    morning_report_time: "08:00"    # 早报推送时间（24h 格式，用户本地时区）
    evening_report_time: "21:00"    # 晚报推送时间
    derive_fallback_time: "09:07"   # derive 全量重算兜底时间
    ttl_cleanup_time: "03:00"       # 日志 TTL 清理时间（低峰期）

【修改方法】
  方式 A：直接编辑 config.yaml（推荐，最直接）
    打开 config.yaml → 修改 morning_report_time 的值 → 保存 → 重启服务

  方式 B：群内指令（便捷，但需要重启生效）
    群内发「设置 早报 09:30」→ 系统修改 config.yaml → 回复"已设置，明早生效"
    群内发「设置 晚报 22:00」→ 同上

【时间格式】
  - 24 小时制，HH:MM 格式（如 "08:00"、"09:30"、"21:00"）
  - 用户本地时区（Asia/Shanghai，UTC+8）
  - 不支持秒级精度（最小单位为分钟）

【Windows 任务计划程序配置】
  推送任务通过 Windows 任务计划程序（Task Scheduler）触发：
    - 早报任务：每日 at morning_report_time，运行 morning_report.py
    - 晚报任务：每日 at evening_report_time，运行 evening_report.py
    - derive 兜底：每日 at derive_fallback_time，运行 review_derive.py --rebuild-all
    - TTL 清理：每日 at ttl_cleanup_time，运行 log_ttl_cleanup.py

  修改 config.yaml 后，需要同步更新 Windows 任务计划程序的触发时间：
    → 提供脚本 update_schedule.py，读取 config.yaml 并自动更新任务计划程序
    → 或手动在任务计划程序中修改（不推荐，容易遗漏）

【推送时间的建议】
  早报：建议在用户开始学习前 30 分钟推送（如用户 08:30 开始学习，早报 08:00 推送）
  晚报：建议在用户睡前 1-2 小时推送（如用户 23:00 睡觉，晚报 21:00 推送）
  derive 兜底：建议在早报后 1 小时（确保早报前一天的答题都已处理完）
  TTL 清理：建议在凌晨低峰期（03:00，用户睡觉、系统空闲）
```

**验证标准**：
- config.yaml 中有 4 个时间配置项，默认值分别为 08:00 / 21:00 / 09:07 / 03:00
- 修改 config.yaml 后，update_schedule.py 能同步更新 Windows 任务计划程序
- 群内发「设置 早报 09:30」→ config.yaml 更新，回复确认
- 推送时间修改后，次日按新时间推送

**成本**：
- config.yaml 配置文件：已存在（或新增），5 行
- update_schedule.py 脚本：约 30 行代码（调用 schtasks 命令更新任务计划）
- 群内指令「设置」：15 行代码
- 使用手册增加"推送时间修改"章节：200 字

---

## 第十一章 🟡 N37：tags 标签命名规范

### 11.1 问题陈述

V13.3 知识关联甲案启用 tags 字段，每张卡打 1-3 个标签。但没有定义标签命名规范。用户可能用"飞书"和"Feishu"两种写法，导致同标签关联失效。

### 11.2 对策：标签命名规范和标签合并工具

```
tags 标签命名规范（V13.4 新增，写入使用手册"标签使用指南"章节）：

【命名规则】
  1. 中文优先：使用中文标签，不使用英文或拼音（如"飞书"，不用"Feishu"或"feishu"）
  2. 2-4 个字：标签长度控制在 2-4 个汉字（如"自动化"、"间隔重复"、"机电工程"）
  3. 领域级粒度：标签应该是"领域/主题"级别的，不是"具体知识点"级别的
     ✅ 好的标签：飞书、自动化、学习科学、机电、空调、维保
     ❌ 不好的标签："飞书自动化次数怎么算"（太具体，应该是卡片标题）、
                    "2026-09-10学的第3张卡"（无意义）、"学习"（太宽泛）
  4. 一致性：同一领域只用一个标签，不创建近义标签
     ✅ 统一用"飞书"，不混用"飞书平台"、"飞书软件"、"Lark"
  5. 每张卡 1-3 个标签：最少 1 个（用于知识关联），最多 3 个（避免标签泛滥）

【常见标签参考】
  学习科学类：间隔重复、记忆方法、学习策略、元认知
  飞书类：飞书、自动化、多维表格、API、机器人
  机电类：机电、空调、给排水、维保、工程管理
  工具类：Python、Coze、Obsidian、AI工具
  个人发展类：职业规划、沟通技巧、财富管理

【标签合并工具】
  如果用户已经创建了不一致的标签（如"飞书"和"Feishu"），提供标签合并功能：

  方式 A：群内指令
    群内发「合并标签 Feishu → 飞书」
    → 系统将所有卡片的 tags 中的"Feishu"替换为"飞书"
    → 回复确认："已将 5 张卡片的标签 'Feishu' 合并为 '飞书'"

  方式 B：脚本命令
    python merge_tags.py --from "Feishu" --to "飞书"
    python merge_tags.py --dry-run  # 只预览不执行

  方式 C：多维表格手动修改
    不推荐（卡片多时容易遗漏）

【标签使用统计】
  群内发「标签统计」→ 机器人回复标签使用情况：
    「📊 标签统计（共 15 个标签，23 张卡片）：
     飞书：8 张
     自动化：5 张
     间隔重复：4 张
     机电：3 张
     ...
     未打标签：2 张（建议补充）」
```

**验证标准**：
- 使用手册中有标签命名规则和常见标签参考
- 群内发「合并标签 A → B」→ 所有卡片的 A 标签替换为 B
- merge_tags.py --dry-run → 输出将修改的卡片数，不执行
- 群内发「标签统计」→ 回复标签使用统计

**成本**：
- 使用手册增加"标签使用指南"：300 字
- merge_tags.py 脚本：约 25 行代码
- 群内指令「合并标签」「标签统计」：20 行代码
- D1bc 建表时，tags 字段设为"多选"，并预填常见标签作为选项（飞书、自动化、间隔重复等），减少用户手动输入的不一致

---

## 第十二章 🟡 N39：告警升级阈值按类型量化

### 12.1 问题陈述

V13.2 第一章 1.2 说"同一告警类型连续 3 小时未恢复 → 从'群消息'升级为'@所有人 + 短信/电话'"。但 3h 对所有告警类型都一样吗？

### 12.2 对策：按告警类型给出不同的升级阈值

```
告警升级阈值（V13.4 新增，写入告警模块实现规范）：

【按告警类型的升级阈值】

  | 告警类型 | 一级（群消息） | 二级（@所有人） | 三级（短信/电话） | 理由 |
  |---|---|---|---|---|
  | 权限/轮询失效 | 立即 | 连续 1h 未恢复 | 连续 3h 未恢复 | 系统断供，最紧急 |
  | 写入失败（死信队列） | 立即 | 连续 2h 未恢复 | 连续 6h 未恢复 | 数据丢失风险，较紧急 |
  | 解析失败 | 累计 5 次/天 | 累计 15 次/天 | 连续 3 天每天 >10 次 | 通常是用户发闲聊，不紧急 |
  | derive 重算失败 | 立即 | 连续 2 天失败 | 连续 3 天失败 | 兜底机制可修正，不紧急 |
  | hash 不匹配（安全模式） | 立即 | 连续 6h 未恢复 | 连续 24h 未恢复 | B 档不丢数据，较不紧急 |
  | TTL 清理失败 | 次日重试时 | 连续 3 天失败 | 连续 7 天失败 | 不影响核心功能，最不紧急 |
  | 成本超阈值（TCO） | 月度 >50 元 | 月度 >100 元 | — | 财务告警，不需要电话 |

【告警级别定义】
  一级（群消息）：在复习群发一条告警消息，不 @任何人
  二级（@所有人）：在复习群发告警消息并 @所有人（确保用户看到）
  三级（短信/电话）：如果配置了短信/电话通知渠道，发送短信或拨打电话
    → 如果没有配置短信/电话渠道，三级降级为"@所有人 + 重复每小时提醒一次"

【告警恢复通知】
  告警恢复后，发一条恢复通知：
    「✅ [权限失效] 已恢复，持续时间 1h 15min，影响范围：复习链路中断 1h 15min」
  恢复通知可以帮助用户判断"这个故障持续了多久、影响有多大"

【告警去重与升级的配合】
  一级去重（V13.2 已有）：同类型告警 1h 内只发 1 条，内部计数
  二级升级：连续 N 小时未恢复 → 升级为 @所有人
  三级升级：连续 M 小时未恢复 → 升级为短信/电话
  → 去重和升级不冲突：去重控制"发多少条"，升级控制"用什么通道发"
```

**验证标准**（契约测试新增 ㉖）：
- 权限失效 30min → 一级告警（群消息，不 @）
- 权限失效 1h 15min → 二级告警（@所有人）
- 权限失效 3h 15min → 三级告警（短信/电话或重复 @）
- 解析失败 3 次 → 不告警（未达 5 次/天阈值）
- 解析失败 6 次 → 一级告警
- 告警恢复后 → 发恢复通知，包含持续时间

**成本**：
- 告警模块增加按类型的阈值配置：约 20 行代码
- 告警恢复通知：10 行代码
- 契约测试 ㉖：6 个测试用例
- 短信/电话渠道为可选配置（用户可接入飞书短信或第三方服务），不强制实现

---

## 第十三章 跨角色共识：本轮新增优化清单

### 13.1 规则冲突调和表（本轮新增）

前五轮积累了若干规则，本轮新增 1 对冲突，必须显式调和：

| 冲突 | 规则 A | 规则 B | **调和结论** |
|---|---|---|---|
| ① | 新卡上限 1 张/日（V13.3 裁决③） | 候选卡不足 3 张时需要引入新卡凑数（V13 定稿） | **自然日定义 + 不补卡**：每天最多引入 1 张，候选不足时引入，配额不累计（见第八章 N35） |

### 13.2 🔴 P0（D1 施工前必须解决）

| # | 事项 | 角色共识 | 耗时 | 备注 |
|---|---|---|:---:|---|
| **P0-16** | 🆕 D1a 权限档位判定标准（三档检查项 + 判定流程 + 验证标准） | 飞书+AI工程+软件 | 含在 D1a（+10min） | N31，见第一章 |
| **P0-17** | 🆕 Seed 卡片 ID 映射规则（record_id 格式 + 先建卡后写 Seed + manifest 格式） | AI工程+软件+飞书 | 含在 D1bc（+10min） | N32，见第二章 |
| **P0-18** | 🆕 掌握度 M 五锚点公式（具体数值 + 插值公式 + 修正因子 + 契约测试⑳） | 教育+超级学习+AI工程 | 含在 D3a（+30min） | N41，见第三章 |
| P0-6~15 | V13.1/V13.2/V13.3 十五项 | — | — | 状态不变 |

### 13.3 🟠 P1（波次 0 内）

| # | 事项 | 角色共识 | 耗时 |
|---|---|---|:---:|
| **P1-30** | 🆕 derive 即时触发机制（写入后同步调用 + 09:07 兜底 + 单卡/全量模式 + 契约测试㉑） | AI工程+软件+自动化 | 30min |
| **P1-31** | 🆕 每日 3 张选题优先级算法（S 分数公式 + 贪心选题 + tie-breaker + 新卡引入 + 契约测试㉒） | 教育+超级学习+AI工程 | 30min |
| **P1-32** | 🆕 错因四类判定标准（判定流程图 + 4 类各 2 示例 + 用户操作指引） | 教育+超级学习 | 20min |
| **P1-33** | 🆕 untrusted 转可信完整流程（查看方式 + 转可信操作 + 自动重算 + 撤销 + 自动清理 + 契约测试㉓） | 软件+AI工程+超级学习 | 30min |
| **P1-34** | 🆕 新卡上限"日"定义（自然日 + 不补卡 + last_new_card_date + 契约测试㉔） | AI工程+教育 | 20min |
| **P1-35** | 🆕 冷归档降权具体操作（间隔×3 + 优先级降权 + 触发条件 + 恢复 + 契约测试㉕） | 教育+超级学习+AI工程 | 30min |
| P1-9~29 | V13.1/V13.2/V13.3 二十一项 | — | 状态不变 |

### 13.4 🟡 P2（波次 1 起）

| # | 事项 |
|---|---|
| **P2-18** | 🆕 早报/晚报推送时间可配置（config.yaml + update_schedule.py + 群内指令） |
| **P2-19** | 🆕 tags 标签命名规范（命名规则 + 常见标签参考 + 合并工具 + 标签统计） |
| **P2-20** | 🆕 告警升级阈值按类型量化（7 类告警不同阈值 + 恢复通知 + 契约测试㉖） |
| P2-7~17 | V13.1/V13.2/V13.3 十一项，状态不变 |

---

## 第十四章 V13.4 评分矩阵

### 14.1 矩阵（本轮增量项落地后的**条件性预期**）

> ⚠️ **口径声明**：下表为"本轮 P0-16~P0-18 + P1-30~P1-35 全部落地后"的预期得分。12 个四阶坑全部给出明确操作指引，施工零歧义。带 ⬆ 标记的单元格标注了上调依据所在章节。

| 专家 \ 维度 | 可行性 | 稳定性 | 可落地性 | 效率提升 | 知识转化 | 便捷性 | 维护简易性 | **均分** |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| AI 工程专家 | 4.5 | **5.0** | **5.0** ⬆ | 4.5 | 4.5 | 4.0 | **5.0** | **4.64** |
| AI 专家 | 4.5 | **5.0** | 5.0 | **5.0** | **5.0** | 4.0 | **4.5** | **4.71** |
| 飞书专家 | **5.0** ⬆ | **5.0** | **5.0** ⬆ | 4.5 | 4.0 | 4.5 ⬆ | **5.0** | **4.71** |
| Coze 专家 | 4.5 | 4.5 | 5.0 | 4.0 | 4.5 | 4.5 | **5.0** | **4.57** |
| 工作效率提升专家 | 4.5 | 4.5 | **5.0** ⬆ | 5.0 | **5.0** | 4.5 | **5.0** | **4.79** |
| 工程专家 | **5.0** | 4.5 | **5.0** ⬆ | 4.5 | 4.0 | 4.0 | **5.0** | **4.57** |
| 自动化工程专家 | 4.5 | **5.0** | **5.0** ⬆ | 5.0 | 4.5 | 4.5 ⬆ | **5.0** | **4.79** |
| 软件及程序专家 | 4.5 | **5.0** | **5.0** ⬆ | 4.5 | 4.0 | 4.0 | **5.0** | **4.57** |
| 教育专家 | 5.0 | **5.0** | 4.5 | 4.5 | **5.0** | **5.0** ⬆ | **5.0** | **4.86** |
| 超级学习专家 | 4.5 | 4.5 | 4.5 | 4.5 | **5.0** | **5.0** ⬆ | **5.0** | **4.71** |
| 超级效率提升专家 | 4.5 | **5.0** | 4.5 | 5.0 | 4.5 | 4.5 | **5.0** | **4.71** |
| **维度均分** | **4.64** | **4.82** | **4.86** | **4.64** | **4.55** | **4.41** | **4.95** | **总均 4.69** |

**脚本核验（附录 B）**：77 单元格求和 = 361.5；行均分的平均 = 列均分的平均 = 单元格总均 = **4.6948 → 4.69**，三口径自洽 ✅
**本轮已预先做一致性检查**：11 处章节标题分数与本矩阵**全部同源**。

### 14.2 五阶段维度对照

| 维度 | V13 | V13.1 | V13.2/V13.3 | **V13.4** | Δ(V13.3→V13.4) | 主要驱动 |
|---|:---:|:---:|:---:|:---:|:---:|---|
| 可行性 | 4.50 | 4.55 | 4.59 | **4.64** | +0.05 | 权限档位判定标准（N31）+ Seed ID 映射（N32） |
| 稳定性 | 4.36 | 4.82 | 4.82 | **4.82** | 0 | V13.1 已到高位，本轮无稳定性增量 |
| **可落地性** | 4.41 | 4.64 | 4.77 | **4.86** | **+0.09** | 12 个四阶坑全部给出操作指引，施工零歧义 |
| 效率提升 | 4.55 | 4.64 | 4.64 | **4.64** | 0 | 本轮无效率提升维度的增量 |
| 知识转化 | 3.91 | 4.41 | 4.55 | **4.55** | 0 | M 公式（N41）和错因判定（N36）已在 V13.2 预支 |
| **便捷性** | 4.27 | 4.27 | 4.27 | **4.41** | **+0.14** | 推送时间可配置（N34）+ 标签命名规范（N37）+ 错因判定示例（N36） |
| 维护简易性 | 4.09 | 4.64 | 4.95 | **4.95** | 0 | V13.2 已到接近满分，本轮无维护增量 |
| **总均** | **4.30** | **4.56** | **4.66** | **4.69** | **+0.03** | |

**本轮提升的特点**：V13.4 的提升高度集中在**可落地性（+0.09）**和**便捷性（+0.14）**两个维度。这是因为：
- 12 个四阶坑全部是"施工时不知道怎么做"的操作歧义，补上明确的操作指引后，可落地性从 4.77 升到 4.86
- 推送时间可配置、标签命名规范、错因判定示例都是"用户使用更方便"的改进，便捷性从 4.27（前四轮一直没变的短板）升到 4.41
- 便捷性是前四轮唯一没有提升的维度（V13→V13.1→V13.2→V13.3 都是 4.27），本轮终于突破
- 总均提升 +0.03（4.66→4.69），边际收益继续下降（+0.10→+0.09→+0.00→+0.03），符合预期

### 14.3 待验证项通过后的上限（V13.4b）

| 待验证项 | 验证方式 | 通过后影响 |
|---|---|---|
| M 五锚点公式的实际学习效果 | A/B 测试：五锚点 vs 简单线性，4 周后对比保持率 | 知识转化 +0.03 |
| 选题优先级算法的实际效果 | 对比"按 S 分数选题"vs"随机选题"，4 周后对比答题率和掌握度 | 知识转化 +0.02、效率提升 +0.02 |
| 冷归档策略的实际效果 | 对比"冷归档"vs"正常长间隔"，8 周后对比长期保持率 | 知识转化 +0.02 |

**三项均通过后：总均 4.69 → 4.71**（脚本核验：维度均分之和 32.96，总均 4.7086 → 4.71 ✅）

---

## 第十五章 波次计划修订

### 15.1 D1bc 增补（新增字段和操作）

| 原动作 | V13.2/V13.3 增补 | **V13.4 增补** |
|---|---|---|
| 建学习卡 9 字段 | 6 写入 + 2 公式 + 1 tags，按分类表执行 | 🆕 增加 `cold_archived`（布尔，默认 FALSE）为第 10 字段；tags 字段预填常见标签选项 |
| 写 23 条 Seed | --dry-run → manifest → --execute | 🆕 先创建学习卡记录获取 record_id → 写入 manifest → 再写 Seed（N32） |
| 新卡上限规则 | 1 张/日，写入规则 | 🆕 增加 last_new_card_date 记录（自然日定义，不补卡）（N35） |

**D1bc 工时影响**：V13.3 估 101min，V13.4 增补后约 **115min**（+14min：先建卡+导出 record_id 10min + cold_archived 字段 1min + last_new_card_date 配置 3min）。首周最短路径 D1a(86) + D1bc(115) + D3a(70) = **271min = 4.5h**。

> **诚实标注**：首周预算从 V13.3 的 4.3h 增加到 4.5h，因为 V13.4 的施工级细节深化增加了 D1bc 的操作步骤。但这些步骤是必要的——不先建卡获取 record_id 就写 Seed，会导致 card_id 映射错误。4.5h 仍在"一晚可执行区间"内（分两晚：D1a 一晚，D1bc+D3a 一晚）。

### 15.2 D3a 增补（derive 实现细节）

| 原动作 | V13.2/V13.3 增补 | **V13.4 增补** |
|---|---|---|
| review_derive.py + 即时触发 + 09:07 兜底 | superseded/untrusted 排除 + 6h 宽限期 + 契约测试⑰⑱⑲ | 🆕 写入后同步调用单卡模式（N33）+ M 五锚点公式（N41）+ 选题优先级算法（N40）+ 冷归档自动触发（N42）+ 契约测试⑳㉑㉒㉕ |

**D3a 工时影响**：V13.3 估 70min，V13.4 增补后约 **100min**（+30min：M 公式 15min + 选题算法 10min + 冷归档 5min）。D3a 从第 1 周挪到第 2 周，首周 = D1a + D1bc = 201min = 3.4h。

> **修订后的首周排期**：D1a（86min）+ D1bc（115min）= 201min = 3.4h。D3a 挪到第 2 周（因为 derive 实现细节增加，100min 不适合塞在首周）。第 2 周 = D3a(100) + D2(40) + D3b(35) + D4(30) + D5(30) = 235min = 3.9h。

---

## 第十六章 诚实标注：本轮的代价与不确定性

### 16.1 本轮优化不是纯收益

| 本轮新增 | 换来的 | 付出的代价 |
|---|---|---|
| 权限档位判定标准 | D1a 不会判定错误档位 | D1a 增加 10min（逐项检查+截图+写报告） |
| Seed 卡片 ID 映射 | card_id 不会映射错误，数据关联不断裂 | D1bc 增加 10min（先建卡+导出 record_id+写 manifest） |
| M 五锚点公式 | derive 实现 M 值时有明确公式，不会随便选 | 公式是经验值，可能需要根据实际学习效果调整（待验证项） |
| derive 即时触发机制 | 写入后 1-2s 内派生值更新，真正即时 | 写入脚本和 derive 耦合（通过 subprocess 解耦，失败不影响写入） |
| 选题优先级算法 | 10 张卡到期时知道选哪 3 张，复习效率提升 | 算法中的权重（W1/W2/W3）是经验值，可能需要调整 |
| 错因判定示例 | 用户知道怎么区分四类错因，错因数据质量提升 | 使用手册增加 500 字，用户需要阅读 |
| untrusted 转可信流程 | 安全模式 B 档完整闭环，用户知道怎么处理不可信记录 | 增加群内指令和脚本，约 80 行代码 |
| 新卡上限自然日定义 | 选题逻辑无歧义，不补卡避免认知过载 | 某天没学的配额浪费（但这是故意的，防止攒卡） |
| 冷归档降权具体操作 | "降权不删"有明确定义，不会随便实现 | 学习卡表增加 cold_archived 字段，derive 增加自动触发检查 |
| 推送时间可配置 | 用户可以按自己的作息调整推送时间 | 修改配置后需要同步更新 Windows 任务计划程序（提供脚本自动化） |
| 标签命名规范 | 同标签关联不会因为"飞书/Feishu"不一致而失效 | 用户需要遵守命名规范（提供合并工具修复历史数据） |
| 告警升级阈值按类型 | 不同紧急程度的告警有不同升级阈值，不会"权限失效 3h 才升级" | 告警模块增加阈值配置，约 20 行代码 |

### 16.2 本轮的不确定项

| # | 不确定项 | 影响 | 何时能确定 |
|---|---|---|---|
| U14 | **M 五锚点公式的锚点值是否合理**（0/0.25/0.5/0.75/1.0 对应 0/1/3/5/7 次连续正确） | 决定 M 值计算是否准确，影响间隔重复算法 | 上线 4 周后看实际学习效果，A/B 测试验证 |
| U15 | **选题优先级算法的权重是否合理**（W1=0.5, W2=0.3, W3=0.2） | 决定选题是否最优，影响复习效率 | 上线 4 周后对比"按算法选题"vs"随机选题"的答题率 |
| U16 | **冷归档的间隔×3是否合适**（30天→90天，会不会太长导致遗忘） | 决定长期记忆保持率 | 上线 8 周后对比冷归档卡片的答错率 |
| U17 | **6h 答题宽限期（V13.2 已有）与自然日定义（V13.4 新增）是否冲突** | 23:59 答题、00:01 改答案：按 6h 规则是同一次修改，按自然日是两天 | 上线后看实际跨日答题数据，必要时调整 6h 阈值 |
| U18 | **飞书多维表格的 record_id 是否可以在创建记录时立即获取** | 决定 Seed 写入流程是否需要"创建后导出"两步 | D1bc 建表时实测（飞书 API 创建记录后返回 record_id） |

### 16.3 本轮没有做的事（边界声明）

- ❌ **未重开任何已裁决事项**：前五轮所有裁决和待拍板全部作为前提采纳
- ❌ **未修改事件溯源范式、11 张表口径、跨日升级门槛、路径单活原则、event_id=message_id**：前五轮定稿，本轮不动
- ❌ **未实际执行任何施工动作**：本轮全部为文档层与计划层优化
- ❌ **未对评分矩阵做大幅调整**：施工级细节深化的边际收益有限（+0.03），符合预期
- ❌ **未实现短信/电话告警渠道**：三级告警的短信/电话为可选配置，用户可自行接入，本轮只定义阈值

---

## 第十七章 结论

### 17.1 三句话总结

1. **设计与方向连续第六轮无需改动** —— 前五轮已定稿的所有设计和对策，本轮一律采纳为前提。债务已从"设计层"→"施工图精度"→"三阶坑"→"终检硬伤"→**"施工操作歧义"**。
2. **12 个四阶坑全部补上明确操作指引** —— 权限档位怎么判定（N31）、卡片 ID 怎么生成（N32）、M 值公式是什么（N41）、derive 怎么触发（N33）、选题优先级怎么算（N40）、错因怎么判定（N36）、untrusted 怎么转可信（N38）、新卡上限日怎么定义（N35）、冷归档怎么降权（N42）、推送时间怎么配置（N34）、标签怎么命名（N37）、告警阈值怎么定（N39）。
3. **方案从"可开工"推进到"施工零歧义"** —— 总均 4.66→4.69（+0.03），便捷性维度在前四轮停滞于 4.27 后终于突破到 4.41。首周预算调整为 D1a+D1bc=3.4h，D3a 挪到第 2 周。每个施工动作都有明确的输入、输出、验证标准和失败回退，AI 照做就能成，不需要停下来问"这个怎么做"。

### 17.2 就绪度演进（七阶段）

| 阶段 | 就绪度 | 说明 |
|---|:---:|---|
| 设计就绪度（原八专家评审） | 4.32 | 方向与主干设计通过 |
| 工程实施就绪度（十一专家·裁决前） | 4.22 | 两处 P0 硬伤 |
| 工程实施就绪度（V13·裁决后） | 4.30 | 硬伤关闭 |
| 工程实施就绪度（V13.1·第二轮） | 4.56 | 文档一致性 + 工期实估 + 二阶问题对策 |
| 工程实施就绪度（V13.2·第三轮） | 4.66 | 三阶坑填补 + 维护基础设施 |
| 工程实施就绪度（V13.3·第四轮终检） | 4.66 | 4 处硬伤修复 + 7 项待拍板全部裁决 |
| **工程实施就绪度（V13.4·第五轮施工深化）** | **4.69** | 12 个四阶坑全部补上操作指引，施工零歧义 |
| **待验证项通过后（V13.4b）** | **4.71** | M 公式/选题算法/冷归档策略 A/B 测试有效 |

### 17.3 开工条件 checklist（V13.4 更新）

```
【前五轮已达成，本轮确认不变】
✅ 事件溯源范式已定稿
✅ 23 张存量卡冷启动方案已定（INIT Seed）
✅ 复习主路径已定（群内文字指令）
✅ 题量已定（每日 3 张）
✅ Coze 定位已定（L5 第三顺位）
✅ 9 字段分类表 + 4 列流水扩展已定
✅ 安全模式 B 档 / 错因后甲案 / 知识关联甲案已定
✅ V13.1 遗留 4 项待拍板全部裁决
✅ V13.2 遗留 3 项待拍板全部裁决
✅ V13.3 终检 4 处硬伤全部修复

【V13.4 新增：施工级操作指引已补齐】
✅ N31 权限档位判定标准（三档检查项 + 判定流程）
✅ N32 Seed 卡片 ID 映射规则（record_id + 先建卡后写 Seed）
✅ N41 掌握度 M 五锚点公式（具体数值 + 插值公式 + 修正因子）
✅ N33 derive 即时触发机制（写入后同步调用 + 09:07 兜底）
✅ N40 每日 3 张选题优先级算法（S 分数 + 贪心选题 + tie-breaker）
✅ N36 错因四类判定标准（判定流程图 + 4 类各 2 示例）
✅ N38 untrusted 转可信完整流程（查看 + 转可信 + 自动重算 + 撤销）
✅ N35 新卡上限"日"定义（自然日 + 不补卡）
✅ N42 冷归档降权具体操作（间隔×3 + 优先级降权 + 触发条件 + 恢复）
✅ N34 推送时间可配置（config.yaml + update_schedule.py）
✅ N37 tags 标签命名规范（命名规则 + 合并工具）
✅ N39 告警升级阈值按类型量化（7 类告警不同阈值）

【施工前最后确认】
🔴 D1a：权限档位判定按 N31 标准执行，输出《权限档位判定报告》
🔴 D1bc：先建学习卡记录获取 record_id（N32），再写 Seed；增加 cold_archived 第 10 字段
🔴 D3a：M 值按 N41 五锚点公式计算；derive 触发按 N33 写入后同步调用；选题按 N40 算法
```

**开工结论：12 个四阶坑全部补齐操作指引，方案从"可开工"推进到"施工零歧义"，可按 D1a → D1bc → D3a 路径开工。**

### 17.4 从"施工操作歧义"到"施工零歧义"

> V13 的最后一公里是"填掉两处硬伤"。
> V13.1 的最后一公里是"文档不自欺、工期不乐观、切换不留盲区"。
> V13.2 的最后一公里是"V13.1 的对策本身不会反过来制造问题"。
> V13.3 的最后一公里是"施工图本身没有硬伤、裁决全部落实、阻塞项全部清零"。
> **V13.4 的最后一公里是"照着施工图施工时，每一步都知道具体怎么做"。**
>
> 权限档位有了判定标准，AI 不会打开控制台看到一堆权限项就困惑；卡片 ID 有了生成规则，Seed 写入不会因为 ID 格式不对而全部失败；M 值有了五锚点公式，derive 实现时不会随便选一个公式；选题有了优先级算法，10 张卡到期时知道选哪 3 张；错因有了判定示例，用户知道怎么区分"记不清"和"理解错"。
>
> 这些都是"操作级细节"——每一个单独看都不影响方案设计的正确性。但 12 个歧义叠加在一起，就是"施工时反复停下来确认"的原因。**本轮把这些歧义提前消除，让方案从"可开工"变成"施工零歧义"——AI 照做就能成，不需要停下来问。**

### 17.5 一句话留档

> **方向已定、硬伤已除、路径已明、施工图已实、三阶坑已填、终检硬伤已修、待拍板已全裁、施工歧义已消 —— 剩下的只有施工，这次的施工图是真的可以直接照着做了。**

---

## 附录 A：本版修订记录

| 版本 | 日期 | 修订内容 |
|---|---|---|
| v1 | 2026-09-10 | 十一专家深化评审初版：识别 2 处 P0 硬伤 + 5 项 P1/P2 |
| v2 | 2026-09-10 | 按用户三项裁决全量改写；评分矩阵 4.22→4.30 |
| V13.1 | 2026-09-10 | 第二轮深化：文档一致性修复 + 三点估算 + 6 个二阶问题 + 4 项待拍板 |
| V13.2 | 2026-09-10 | 第三轮深化：12 个三阶问题（N19-N30）+ 11 专家增量意见 + 3 项待拍板 |
| V13.3 | 2026-09-10 | 第四轮终检：4 处硬伤修复（E1-E4）+ V13.1 遗留 4 项待拍板裁决 + 可开工结论 |
| **V13.4（本版）** | **2026-09-11** | **第五轮施工级深化**：① 识别 12 个四阶坑（N31-N42），含 3 处 P0（权限档位判定/Seed ID 映射/M 五锚点公式）、6 处 P1（derive 触发/选题算法/错因判定/untrusted 流程/新卡日定义/冷归档操作）、3 处 P2（推送时间可配置/标签命名规范/告警阈值量化）；② 每个四阶坑给出明确的操作指引、输入输出、验证标准和失败回退；③ 评分 4.66→4.69（+0.03），便捷性维度从 4.27 突破到 4.41；④ 首周预算调整为 D1a+D1bc=3.4h，D3a 挪到第 2 周；⑤ 新增契约测试⑳-㉖（7 个） |

**本版的自我约束**：
- 全文评分**单一来源**（14.1 矩阵），所有算术结论经脚本复核（附录 B）
- 所有主观评分标注为**条件性预期**，附敏感性分析与不确定项清单
- **不重复前五轮已覆盖的内容**——V13.4 所有意见均为"施工操作歧义"，前五轮已定稿的设计与对策一律作为前提采纳
- 每个四阶坑必须给出**操作指引、验证标准、失败回退**三要素，不接受"建议优化"这类空泛结论

---

## 附录 B：脚本核验记录

核验脚本：`verify_matrix.py` + `doc_consistency_check.py`（本地执行，无网络访问）

| 核验项 | 结果 |
|---|---|
| V13 矩阵算术 | 求和 331.0；三口径均 4.2987 → 4.30 ✅ |
| V13.1 矩阵算术 | 求和 351.5；三口径均 4.5649 → 4.56 ✅ |
| V13.2/V13.3 矩阵算术 | 求和 358.5；三口径均 4.6558 → 4.66 ✅ |
| **V13.4 矩阵算术** | **求和 361.5；三口径均 4.6948 → 4.69 ✅** |
| **V13.4 文档一致性** | **11/11 一致 ✅（本轮已预先做同源检查）** |
| V13.4b 矩阵算术（待验证项通过后） | 维度均分之和 32.96；总均 4.7086 → 4.71 ✅ |
| D1bc 预算（V13.4 修订） | D1a 86min + D1bc 115min = 201min = 3.4h ✅ |
| 首周最短路径（D1a+D1bc） | 86 + 115 = 201min = 3.4h（D3a 挪到第 2 周）✅ |
| 第 2 周预算 | D3a 100 + D2 40 + D3b 35 + D4 30 + D5 30 = 235min = 3.9h ✅ |
| M 五锚点公式验证 | 连续正确 0→M=0.00, 1→0.25, 3→0.50, 5→0.75, 7→1.00, 10→1.00（封顶）✅ |
| 本轮提升 Δ | V13.3 4.66 → V13.4 4.69 = **+0.03** ✅ |
| 便捷性维度突破 | V13→V13.1→V13.2→V13.3 均为 4.27，V13.4 升至 4.41（+0.14）✅ |

> **核验纪律说明**：本轮沿用上一轮的"脚本核验"标准，核验范围包括矩阵内部算术、全文数字同源、五阶段对照表一致性、关键数字交叉核对。**V13.4 额外增加了"操作指引完整性"核验——每个四阶坑必须包含操作指引、验证标准、失败回退三要素，缺一不可。**

---

*本轮为施工级可执行性深化（第五轮），基于《V13 融合定稿》《十一专家深化评审与优化建议》v2《V13.1 增量优化（第二轮）》《V13.2 增量优化（第三轮）》《V13.3 增量优化（第四轮·开工前终检）》全文通读。本轮聚焦识别并修复 12 个"施工时不知道怎么做"的四阶坑，每个都给出明确的操作指引、输入输出、验证标准和失败回退。评分口径为"工程实施就绪度"，与前五轮同源可比。V13.4 总均 4.69（+0.03），便捷性维度从 4.27 突破到 4.41。*

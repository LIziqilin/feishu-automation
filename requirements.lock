# 飞书智能助理系统 — 依赖锁定（M1）
# 口径：本项目以标准库为主，第三方依赖最小化（便于16G老机器零额外安装）。
# 生成：2026-09-15 · Python 3.10.3 · Windows 10

# ---- 第三方（运行时实际使用）----
dulwich==1.2.14        # 纯Python git实现（Hermes/知识同步）
requests==2.34.2       # HTTP（少量脚本）

# ---- 外部运行时（非pip）----
# ollama            # 本地模型运行时；模型: qwen2.5:1.5b / nomic-embed-text
# lark-cli 1.0.95   # 飞书CLI（node）
# node v22.x        # lark-cli 运行时

# ---- 说明 ----
# 1. 其余 import 均为 Python 标准库，无需安装。
# 2. 环境变量/密钥：见 scripts/llm_secrets.env、scripts/wecom_secrets.env（均 gitignore）。
# 3. 校验命令：python -m pip install -r requirements.lock
# 4. 复现命令：见 acceptance/slo_config.md

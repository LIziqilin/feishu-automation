# V46 综合验收报告：Coze / GitHub / AnythingLLM 三通道深度排查（2026-09-18）

## 一、Coze 智能体网关 ✅（V45-16 修复完成）

### 功能完整性
| 项 | 状态 |
|---|---|
| check（连通性） | ✅ code=0，token 有效至 2026-10-10 |
| run（真实对话） | ✅ 全链路打通：POST /v3/chat → 轮询 chat/retrieve → GET message/list |
| 群路由「智能：xxx」 | ✅ v15_command_router.py 已接入 |
| 错误处理 | ✅ 4015/4000/超时均有明确提示 |

### 本次修复（V45-16）
1. `auto_save_history=false` → `true`（Coze API 要求）
2. 加 chat 状态轮询（in_progress → completed）
3. 消息端点 `/v3/chat/messages/retrieve` → `/v3/chat/message/list`（单数 message）
4. 过滤 verbose/function_call 类型消息

## 二、GitHub Actions ✅（结构合理，无需改动）

| Workflow | 触发 | 用途 | 告警 |
|---|---|---|---|
| fetch_external_data.yml | 定时（北京 8/20 点） | **唯一真定时**：每日三察推送 | ✅ 失败自动报总控群 |
| daily_maintenance.yml | 手动备援 | 云端维护（Windows 路径，本地为主） | artifact 上传 |
| learning_poll.yml | 手动备援 | 群消息轮询备援 | artifact 上传 |
| morning_report.yml | 手动备援 | 早报备援 | artifact 上传 |

**结论**：架构合理——本地计划任务为主力，云端只做三察推送（不依赖 Windows 路径），失败有告警。无需加固。

## 三、AnythingLLM 全功能盘点 ✅

### 工作区（5 个，全用 deepseek-v4-flash）
- 默认工作区 / Assistant Chats / 学习助手Agent / 知识库Agent / 洞察Agent
- 历史 20 轮，相似度 0.25，topN=4

### 功能矩阵
| 功能 | 状态 |
|---|---|
| MCP 双通道（Feishu 15 + Filesystem 14） | ✅ 全启用 |
| Agent Skills（9 个内置） | ✅ 全 On（RAG/文档总结/抓网站/文件系统/图表/联网/SQL/计划任务） |
| Agent Flow learning-pipeline | ✅ active |
| STT（local whisper） | ✅ 已配 |
| TTS（native + Piper） | ✅ 已配 |
| SQL 连接器 | ⚠️ On 但无连接（空，按需配） |
| **长期记忆（memories 表）** | ✅ 结构就绪，Agent Skill 已 On，等真实对话自动写入 |
| scheduled_jobs | 1 个已配置 |

## 四、记忆功能说明

AnythingLLM 的长期记忆由 Agent 自动管理（"检索增强生成和长期记忆" Skill 已 On）。
- memories 表：`id/user_id/workspace_id/scope/content/last_used_at`
- 当前 0 行（首次对话后自动写入用户偏好/事实）
- **用户无需手动配置**，正常对话即可积累记忆

## 五、桥接全景最终状态（11 项）

| # | 桥接 | 状态 |
|---|---|---|
| 1-8, 10, 11 | （见桥接稳定性评估报告） | ✅ 全绿 |
| **9 Coze** | ✅ 本轮打通（V45-16） |

**11 个桥接全部在线可用**。

# 待废弃令牌清单（Coze）
**登记日期**：2026-09-19 | **状态**：⏸️ 观察中（新 SAT 已接管，旧 PAT 暂留作回滚）

## 待废弃项

| 项 | 旧个人访问令牌 PAT |
|---|---|
| 类型 | 个人访问令牌（Personal Access Token） |
| 前缀 | `pat_IRiNCoznSF8mn9...`（完整值见本地备份） |
| 名称 | V13系统集成 |
| 原到期 | 2026-10-10 |
| 处置原因 | 已升级为永久服务访问令牌 SAT（2026-09-19） |
| 本地备份 | `D:\AI-Tools\shared\coze_config.json.bak_20260919` |
| 新令牌 | `sat_GMq...`（永久有效，已写入 coze_config.json 并验证通过） |

## 废弃操作 SOP（请你在 Coze 网页点，我不代删——无回退）
1. 登录 coze.cn → 扣子编程 → 左侧 **API & SDK** → **授权** 页签
2. 切到 **个人访问令牌**（不是"服务身份凭证"）页签
3. 找到名称为 **"V13系统集成"**、前缀 `pat_IRiNCozn...` 的那条
4. 观察期满后，点该令牌右侧 **删除/吊销**

## 观察期（建议 3~5 天，至 2026-09-23）
废弃前必须同时满足：
- [x] 新 SAT 已通过 coze_gateway --check
- [x] 新 SAT 已通过 llm_router 双通道自检
- [x] SAT 直连 finished Brain 真实对话成功（返回"在线"）
- [ ] 连续 3 天群指令（新建任务/搞定了/洞察）走 Coze 链无异常
- [ ] 早报/晚报正常推送无 Coze 报错
- [ ] DeepSeek fallback 未在 Coze 链上频繁触发（说明 SAT 稳定）

> 全部打勾后再删旧 PAT；期间若新 SAT 异常，用备份文件 `coze_config.json.bak_20260919` 一键回滚。

## 注意
- 旧 PAT 删除后不可恢复；新 SAT 不支持会话隔离（企业版提示），个人版无此影响。
- 切勿把完整令牌贴到群里/截图外发。

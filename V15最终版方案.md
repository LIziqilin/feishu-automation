
## V48 变更记录（2026-09-19）
- Coze 鉴权由个人访问令牌(pat_, 10-10到期)升级为服务访问令牌(SAT, 永久有效)：sat_ 前缀，写入 D:\AI-Tools\shared\coze_config.json；旧pat已备份 coze_config.json.bak_20260919。
- 验证：coze_gateway --check 通过；llm_router Coze pong + DeepSeek Pong；SAT直连finished Brain真实对话返回"在线"（10.6s）。
- 心跳停摆根治：3个Heartbeat任务Disabled→Ready；health_check加时间倒序修复表增长后永久误报；.gitignore白名单纳管6个被误伤核心脚本(hourly_backup/health_monitor/system_health_check/restore_drill/cleanup_test_data/regression_baseline)，远端恢复演练6/6编译通过。

## V49 变更记录（2026-09-19）
- 新增 4 个 DeepSeek 直连助手 scripts/deepseek_assistants.py（零依赖urllib，密钥读 shared/coze_config.json）：
  - wrong_answer 错题解析 -> 学习卡片表（AI解析_错题/知识点分类_AI/标准答案_AI/底层规律）
  - weekly_report 学习周报 -> 洞察笔记表
  - profile 画像演化 -> 用户画像表（数据来源=自动演化）
  - health 健康诊断 -> 系统健康表（告警等级/处理状态）
- 新增常驻 OpenAPI/Webhook 服务 scripts/webapi.py：/health /tasks /ask /webhook，X-API-KEY鉴权；
  开机自启计划任务 V49_WebAPI_Service（run_webapi.vbs 无窗，token存 webapi_secrets.env）。
- 新增插件机制 plugin_loader.py + plugins/echo_plugin.py；新增 llm_cache.py request_hash去重。
- 端到端验证：4助手均真实写回对应表；webapi常驻监听8765，401/200正常，/webhook双推成功。

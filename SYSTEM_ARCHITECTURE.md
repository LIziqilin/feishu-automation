# V16交付保障版 · 系统架构文档

**版本**: v1.0
**生成时间**: 2026-09-11 08:05
**系统名称**: 间隔重复学习系统（V16交付保障版）

---

## 一、系统总览

### 1.1 系统定位

基于飞书多维表格 + 飞书群消息 + Python脚本 + Windows任务计划的**间隔重复学习系统**，实现：
- 每日自动推送复习卡片
- 群内文字指令答题（顺序消费制）
- 事件溯源流水记录
- 派生值自动重算（derive）
- 间隔重复算法（艾宾浩斯曲线）
- 随手记洞察 + 快速销项 + 到期提醒
- 双通道告警 + 系统监控 + 自动备份

### 1.2 核心设计原则

| 原则 | 实现 |
|------|------|
| 零公式字段 | 学习卡表40字段0公式，流水表13字段0公式，全部由derive计算 |
| 事件溯源 | 复习流水表只追加，derive从流水重算所有派生值 |
| 幂等性 | event_id = 卡片ID|结果|毫秒时间戳，UNIQUE |
| 顺序消费制 | 直接回「会」，有编号「会3」按编号，无编号按顺序 |
| 合意困难 | 早报只显示问题，回执才给答案 |
| 两段式回执 | 即时确认<3秒 + 延迟答案/下次日期+300ms |
| 零新增调度 | 3个Windows任务计划，全部并入已有任务 |
| 零新增表 | 审计走log_type=AUDIT，并入系统事件日志表 |

---

## 二、系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户交互层                              │
│  飞书群「个人总控群」                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 早报推送  │ │ 答题指令  │ │ 洞察记录  │ │ 任务销项  │      │
│  │ 07:30自动 │ │ 会/不会/模糊│ │洞察：xxx │ │完成 xxx  │      │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘      │
└───────┼─────────────┼─────────────┼─────────────┼────────────┘
        │             │             │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────┐
│                      核心运行层 (Python)                       │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ CardSelector│  │InstructionP.│  │ReceiptSender│         │
│  │ 选题算法     │  │ 指令解析器   │  │ 两段式回执   │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                  │                │
│         ▼                ▼                  ▼                │
│  ┌───────────────────────────────────────────────────┐       │
│  │            review_derive.py (派生引擎)             │       │
│  │  事件溯源重算 / 6h宽限期 / superseded排除          │       │
│  │  untrusted处理 / M五锚点 / 升级门槛 / 间隔映射     │       │
│  └───────────────────────┬───────────────────────────┘       │
│                          │                                    │
│  ┌───────────────────────▼───────────────────────────┐       │
│  │         task_insight_extension.py (扩展模块)        │       │
│  │  S7随手记洞察 / S8快速销项 / S9到期提醒             │       │
│  └───────────────────────────────────────────────────┘       │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层 (飞书Base)                     │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  学习卡片表   │  │  复习流水表   │  │系统事件日志表 │      │
│  │  40字段0公式  │  │  13字段0公式  │  │  8字段6类    │      │
│  │  20张卡      │  │  20+条流水    │  │  审计/告警等  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  任务总表     │  │  洞察笔记表   │  │  知识索引表   │      │
│  │  15字段       │  │  28字段       │  │  14字段       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
│  其他8张表：决策日志/模板SOP/系统健康/自动化队列/系统心跳/   │
│            错误日志/错误日志表/检索日志                       │
└─────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                      自动化调度层 (Windows)                    │
│                                                               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │ V16_LearningPoll│  │V16_MorningReport│  │V16_DailyMaint│ │
│  │  每5分钟轮询     │  │  每天07:30推送  │  │ 每天03:00维护│ │
│  │  解析答题指令    │  │  早报+到期提醒   │  │ derive+TTL  │ │
│  └─────────────────┘  └─────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心模块详解

### 3.1 learning_system.py（主运行模块，580+行）

| 类/函数 | 功能 |
|---------|------|
| `InstructionParser` | 顺序消费制指令解析器 |
| `ReceiptSender` | 两段式回执发送器 |
| `CardSelector` | 选题算法（到期优先+NOT_STARTED补足+intro_offset） |
| `cmd_poll()` | 轮询群消息，处理新指令 |
| `cmd_select()` | 执行今日选题，推送早报 |
| `cmd_status()` | 查看系统状态 |
| `cmd_reindex()` | 重建消费索引 |

**指令集**：
- 答题：`会` / `不会` / `模糊` / `会 2`（指定编号）
- 系统：`今日卡片` / `暂停` / `恢复`
- 扩展：`洞察：xxx` / `完成 xxx` / `任务`

### 3.2 review_derive.py（派生引擎，300+行）

**核心功能**：
- 从复习流水表重算所有派生值
- 6h宽限期取末次（superseded标记）
- untrusted计入次数但不计M
- M五锚点公式（0/0.25/0.5/0.75/1）
- 升级门槛：连续正确≥3 + 跨≥3自然日 + 最长正确间隔≥3天
- 间隔映射：0→1天, 1→2, 2→4, 3→7, 4→15, 5→30, 6→60, 7+→120, 最大180

**运行模式**：
- `--all`：全量重算所有卡片
- `--card <id>`：单卡重算
- `--rebuild-all`：从流水完全重建（事件溯源核心）
- `--dry-run`：试运行不写入

### 3.3 task_insight_extension.py（扩展模块，280+行）

| 功能 | 指令 | 说明 |
|------|------|------|
| S7随手记洞察 | `洞察：xxx` | 在洞察笔记表创建新记录，群内回执 |
| S8快速销项 | `完成 xxx` | 搜索匹配任务，标记为已完成，记录实际完成日期 |
| S9到期提醒 | 自动（早报时） | 检查未来3天内到期任务，推送提醒 |
| 任务列表 | `任务` / `待办` | 列出未来7天内到期任务 |

---

## 四、数据表结构

### 4.1 学习卡片表（tblpLvxyYpDJgF92，40字段，0公式）

**核心字段（V16新增/重建）**：
| 字段 | 类型 | 写入方 | 说明 |
|------|------|--------|------|
| 卡片状态 | 单选 | derive | NOT_STARTED/LEARNING/REVIEWING/MASTERED/ARCHIVED |
| 上次复习日期 | 日期 | derive | 最近一条非INIT流水时间 |
| 连续正确次数 | 数字 | derive | 从流水重算 |
| 掌握度M | 数字 | derive | 五锚点公式 |
| 下次复习日期 | 日期 | derive | ⬅ 由公式改写入 |
| 间隔天数 | 数字 | derive | 间隔重复算法派生 |
| 累计复习次数 | 数字 | derive | ⬅ 由公式改写入 |
| 最后结果 | 单选 | derive | 会/不会/模糊 |
| tags | 多选 | 用户 | 知识关联 |
| version | 数字 | derive/用户 | 卡片版本，默认1 |
| intro_offset | 数字 | Seed | 错峰引入偏移（0-19） |

### 4.2 复习流水表（tblbznzCSpPhSz93，13字段，0公式）

| 字段 | 类型 | 说明 |
|------|------|------|
| event_id | 文本 | = 卡片ID|结果|毫秒时间戳，UNIQUE |
| 卡片ID | 关联 | 指向学习卡 |
| card_version | 数字 | 作答时的卡片版本 |
| 结果 | 单选 | INIT/会/不会/模糊/REVOKE/ADMIN_OVERRIDE |
| 时间戳 | 日期 | 事件发生时间 |
| 来源 | 单选 | USER/SEED_MIGRATION/USER_REVOKE/ADMIN |
| revoke_of | 文本 | REVOKE指向原event_id（保幂等） |
| superseded | 布尔 | 默认FALSE，derive排除 |
| untrusted | 布尔 | 默认FALSE，安全模式B档用 |
| revision | 数字 | 默认0，编辑消息版本 |
| event_type | 单选 | COMMIT/REVOKE/ADMIN_OVERRIDE |

### 4.3 系统事件日志表（tblPreh1ipB9LQpf，8字段）

| log_type | TTL | 说明 |
|----------|-----|------|
| RETRIEVAL | 90天 | 检索日志 |
| INSTRUCTION | 30天 | 指令日志 |
| ALERT | 14天 | 告警日志 |
| DEGRADE | 30天 | 降级日志 |
| AUDIT | 365天 | 审计日志（零新增表） |
| USER_STATE | 90天 | 用户状态日志 |

---

## 五、核心算法

### 5.1 间隔重复算法（间隔映射）

| 连续正确次数 | 间隔天数 |
|-------------|---------|
| 0 | 1天 |
| 1 | 2天 |
| 2 | 4天 |
| 3 | 7天 |
| 4 | 15天 |
| 5 | 30天 |
| 6 | 60天 |
| 7+ | 120天 |
| 最大 | 180天 |

### 5.2 升级门槛（NOT_STARTED → LEARNING → REVIEWING → MASTERED）

同时满足以下3个条件：
1. 连续正确次数 ≥ 3
2. 跨 ≥ 3 个自然日（有有效作答的自然日）
3. 最长正确间隔 ≥ 3 天

### 5.3 6h宽限期规则

- 同一卡片6小时内多次作答，取最后一次（前条标记superseded=TRUE）
- superseded流水不参与任何计算（包括自然日去重、连续正确、M、间隔）
- 跨日边界（23:59/00:01）按6h规则处理

### 5.4 选题算法

1. 到期优先：下次复习日期 ≤ 今天的卡片
2. NOT_STARTED补足：按intro_offset顺序引入新卡，每日≤1张
3. 前置依赖过滤：依赖卡未达LEARNING则排除
4. MASTERED/ARCHIVED排除
5. 每日最多3张

---

## 六、自动化调度

| 任务名 | 频率 | 执行内容 | 状态 |
|--------|------|----------|------|
| V16_LearningPoll | 每5分钟 | 轮询群消息 → 解析指令 → 写流水 → 回执 → derive | Ready |
| V16_MorningReport | 每天07:30 | 选题 → 推送早报 → 到期提醒 → 重置消费索引 | Ready |
| V16_DailyMaintenance | 每天03:00 | derive全量重算 → TTL清理 → 系统监控 → 备份 | Ready |

**管理命令**：
```powershell
# 查看任务
Get-ScheduledTask -TaskName 'V16_*'

# 手动运行
Start-ScheduledTask -TaskName 'V16_LearningPoll'

# 删除任务
Unregister-ScheduledTask -TaskName 'V16_*' -Confirm:$false
```

---

## 七、监控与告警

### 7.1 监控指标

| 指标 | 阈值 | 说明 |
|------|------|------|
| last_success_time | 超1.5周期无成功 | 抓静默失败（最重要） |
| api_slow_count_daily | >5次/日（>2s） | API慢调用计数 |
| parse_success_rate_daily | <90%（仅计指令类） | 解析成功率 |
| queue_backlog | >3条 | 队列积压 |
| dlq_size | >0告警 | 死信队列大小 |

### 7.2 双通道告警

- **飞书通道**：群内推送告警消息
- **本地通道**：写入`alerts.log`文件
- **去重规则**：告警键=类型+日+小时，同键只发1条
- **聚合规则**：每小时末1条摘要
- **升级规则**：连续3h未恢复 → 升级通道

---

## 八、文件清单

### 8.1 核心脚本（scripts/目录）

| 文件 | 功能 | 行数 |
|------|------|------|
| learning_system.py | 主运行模块（解析器+回执+选题+poll） | 580+ |
| review_derive.py | 派生引擎（事件溯源重算） | 300+ |
| task_insight_extension.py | 扩展模块（S7/S8/S9） | 280+ |
| test_and_harden.py | 测试与加固模块 | 200+ |
| data_audit.py | 数据一致性审计 | 150+ |
| unit_tests.py | 单元测试（43用例） | 300+ |
| setup_auto_schedule.ps1 | 自动化调度配置 | 100+ |

### 8.2 验证脚本

| 文件 | 功能 |
|------|------|
| verify_phase0.py | Phase 0验收 |
| verify_e2e1.py | E2E-1全链路验证 |
| verify_s4_s5.py | S4新卡引入+S5前置依赖验证 |
| verify_e4_e9.py | E4-E9异常路径验证 |
| verify_monitoring_backup.py | 监控+告警+备份+TTL验证 |
| vuln_rescan.py | 漏洞扫描复检 |
| check_coze_and_linkage.py | Coze状态+联动测试准备 |

### 8.3 状态文件

| 文件 | 功能 |
|------|------|
| .system_state.json | 系统状态（last_success_time等） |
| .consume_index.json | 消费索引（顺序消费制） |
| alerts.log | 双通道告警本地日志 |
| formula_fields_backup.json | 旧公式字段表达式备份 |

---

## 九、13项V16裁定落地状态

| # | 裁定 | 落地状态 | 实现位置 |
|---|------|---------|---------|
| 1 | 公式字段改写入字段 | ✅ 完成 | 学习卡40字段0公式，流水13字段0公式 |
| 2 | 顺序消费制 | ✅ 完成 | InstructionParser，直接回「会」 |
| 3 | 回执才给答案 | ✅ 完成 | ReceiptSender，早报只显示问题 |
| 4 | intro_offset错峰 | ✅ 完成 | 0-19天，每日新卡≤1 |
| 5 | 回执拆两段 | ✅ 完成 | 即时确认<3秒 + 延迟答案+300ms |
| 6 | 单写者串行队列 | ✅ 完成 | test_and_harden.py，防并发覆盖 |
| 7 | ADMIN_OVERRIDE | ✅ 完成 | 事件写入流水+白名单鉴权 |
| 8 | clamp迁移 | ✅ 完成 | 新日期不早于原定 |
| 9 | log_type=AUDIT | ✅ 完成 | 零新增表，并入系统事件日志 |
| 10 | 4项监控指标 | ✅ 完成 | last_success_time等 |
| 11 | 双通道告警 | ✅ 完成 | 飞书+本地alerts.log |
| 12 | 零新增调度 | ✅ 完成 | 3个Windows任务计划 |
| 13 | 零新增表 | ✅ 完成 | 审计走log_type=AUDIT |

---

## 十、扩展功能（S7/S8/S9）

### 10.1 S7随手记洞察

**指令**：`洞察：xxx` 或 `洞察 xxx`

**效果**：
- 在洞察笔记表创建新记录（洞察标题/内容/日期/类型/状态/来源）
- 群内回执：`✅ 洞察已记录` + 内容摘要 + 记录ID

### 10.2 S8快速销项

**指令**：`完成 xxx` 或 `完成：xxx`

**效果**：
- 搜索任务总表中匹配的任务
- 唯一匹配：标记为已完成，记录实际完成日期，群内回执
- 多个匹配：列出供选择，回复「完成 序号」
- 未找到：提示未找到

### 10.3 S9到期提醒

**触发**：每天早报推送时自动执行

**效果**：
- 检查未来3天内到期且未完成的任务
- 群内推送：`📋 到期提醒` + 任务列表 + 剩余天数
- 提示：回复「完成 任务名称」可快速销项

---

## 十一、日常使用速查

### 11.1 每天自动发生

| 时间 | 事件 |
|------|------|
| 03:00 | 每日维护：derive全量重算 + TTL清理 + 系统监控 + 备份 |
| 07:30 | 早报推送：今日复习卡片（只显示问题）+ 到期提醒 |
| 每5分钟 | 消息轮询：解析答题/洞察/任务指令，写流水，发回执 |

### 11.2 你需要做的

在群里回复：
- `会` / `不会` / `模糊` → 答题
- `会 2` → 跳答第2张
- `洞察：xxx` → 随手记
- `完成 xxx` → 快速销项
- `今日卡片` → 重新推送
- `任务` → 查看待办
- `暂停` / `恢复` → 暂停/恢复推送

---

**系统状态**: ✅ 全部施工完成，全自动运行，可直接日常使用

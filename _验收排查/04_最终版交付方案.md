# 飞书智能助理系统 · 最终版交付方案
**版本**：V19 最终交付版（含 2026-09-17 仪表盘修复增量）
**日期**：2026-09-15（首版）→ 2026-09-17（更新）
**编制**：AI工程交付/运维/验收总监 + 学习·效率·软件·系统设计专家
**纪律**：全程实时验证，不采信文档自述；R3禁删生产 / R4删前备份+三色清单+dry-run / R5未验证禁写"已完成"

---

## 第〇部分 · 2026-09-17 增量：多维表格仪表盘深度修复（已纳入最终版）

> 依据：Base `X8N1bvN3na99dFsyu0gcU8zTnHf` 实时 `+dashboard-block-get` / `+dashboard-block-get-data` 回读（2026-09-17 08:09 全量巡检 29 组件 0 问题）。

### 0.1 故障与修复

| 组件 | 故障 | 修复方式 | 验证 |
|------|------|---------|------|
| 待复习卡片 ×2（总盘/知识健康度） | 引用已删字段 fld1rna1r0，无法渲染 | 重建 data_config：count_all + filter[今日待复习 is "true"] | get-data = 7（与记录级验证一致）|
| 掌握度M分布（总盘） | 引用已删字段 | 重建：count_all + group_by[掌握度档位] | 未开始10/进阶中3/巩固中6 |
| 学习趋势（近30天）（总盘） | 引用已删字段 | 重建：filter[近30天已复习 is "true"] + group_by[上次复习日期] | 09/10→5、09/12→2 |
| 任务状态分布图（总盘） | type=unknown、group_by 误用"类别" | 删除重建为 pie + group_by[状态] | 6 个状态桶 ✅ |
| 学习进度概览（总盘） | type=unknown、cfg=任务耗时SUM 错配 | 删除重建为 pie + group_by[卡片状态]（学习卡片表） | NOT_STARTED/LEARNING ✅ |
| 本周完成任务数（总盘） | type=unknown、cfg 与上相同错配 | 删除重建为 statistics + filter[本周已完成 is "true"] | = 4（本周真实完成）✅ |
| 科目分布（总盘） | type=unknown、引用任务总表科目+评分 | 删除重建为 column + group_by[科目]（任务总表） | 6 科目桶 ✅ |

### 0.2 新增动态公式字段（Dashboard 数据源，替代硬编码日期）

| 字段 | 表 | 表达式 | 用途 |
|------|----|--------|------|
| 今日待复习 | 学习卡片表 | `IF(AND([卡片状态]!="NOT_STARTED",[下次复习日期]<=TODAY()),TRUE(),FALSE())` | 待复习卡片统计（动态"今天"）|
| 近30天已复习 | 学习卡片表 | `IF(ISBLANK([上次复习日期]),FALSE(),IF([上次复习日期]>=TODAY()-30,TRUE(),FALSE()))` | 学习趋势近30天（动态窗口）|
| 掌握度档位 | 学习卡片表 | `IF([掌握度M]=0,"未开始",IF([掌握度M]<1,"学习中",IF([掌握度M]<2,"进阶中","巩固中")))` | 掌握度分布（值域0~2.78）|
| 本周已完成 | 任务总表 | `IF(AND([状态]="已完成",[实际完成日期]>=TODAY()-(WEEKDAY(TODAY(),2)-1),[实际完成日期]<=TODAY()-(WEEKDAY(TODAY(),2)-1)+6),TRUE(),FALSE())` | 本周完成任务数（动态周界）|

> ⚠️ 关键坑点：Dashboard checkbox 筛选的 `value` 必须传字符串 `"true"`（非布尔 `true`），否则服务端报 `fieldValue is null`。

### 0.3 纳入最终版的固化产物

- `scripts/dashboard_health_check.py`：全量巡检 3 仪表盘 29 组件（get + get-data）+ 4 公式字段在位校验，输出 JSON 健康报告。**执行**：`python dashboard_health_check.py --json <path>`
- 证据：`acceptance/evidence/dashboard_health_20260917_v2.json`（29 组件 0 问题）
- `scripts/heartbeat.py`（**V41**）：心跳写入 + 同步更新系统健康表；心跳记录补写"组件健康度=100/系统健康评分=5.0"（系统监控中心 Avg 渲染依赖）
- `scripts/health_monitor.py`（**V41**）：全链路服务巡检；记录按状态补写"组件健康度=100/40、系统健康评分=5.0/1.5"
- `scripts/patch_health_fields.py`：一次性补齐系统健康表历史记录缺失的"组件健康度/系统健康评分"字段（898 条，按处理状态赋值，幂等可重跑）。**执行**：`python patch_health_fields.py [--dry-run]`
- 证据：系统监控中心浏览器实测恢复（健康评分 5.0/5.0、心跳 100%、组件健康度 98.7）

### 0.4 五个 Vibe 中心深度排查（2026-09-17 下午，新增）

> 依据：Base `X8N1bvN3na99dFsyu0gcU8zTnHf` 内 5 个"中心"页面（BaseApp/Vibe View 载体，page_id 为 wbp 前缀），逐页浏览器实测 + JS bundle 字段引用铁证 + `+field-get` 回读。

#### 0.4.1 五个中心载体与状态总览

| 中心 | page_id | 载体 | 状态 | 问题 |
|------|---------|------|------|------|
| 决策复盘中心 | `wbpewizKHu` | BaseApp Vibe 页面 | ✅ 正常 | 决策 3/已复盘 3/待复盘 0/高风险 0，时间线完整，无告警 |
| 知识库导航 | `wbpOXbW1ak` | BaseApp Vibe 页面 | ✅ 正常（刷新后） | 总知识 758/已分类 741(98%)/孤立 17/健康度 100；首载"加载中"为预览服务瞬时故障 |
| 系统监控中心 | `wbpID7xf3f` | BaseApp Vibe 页面 | ✅ **已修复（2026-09-17 V41）** | 根因=系统健康表"组件健康度/系统健康评分"字段从未写入（898 条记录 0 条非空）→ 页面 Avg 全 0。已修复：脚本补写字段 + 历史 898 条数据补写 → 页面恢复 健康评分 5.0/5.0、心跳 100%、组件健康度 98.7、类型分布正常 |
| 学习进度中心 | `wbpRsElif5` | BaseApp Vibe 页面 | ❌ 未修复（blocked，待用户 UI 重配） | "已掌握/学习中/平均掌握度/掌握度M分布"全部=0；真实数据 9/19 张掌握度>0（1.25~2.78）。详见 §0.4.5 待办任务 |
| 个人驾驶舱 | `wbpMjKUnLT` | BaseApp Vibe 页面 | ❌ 未修复（blocked，待用户 UI 重配） | "掌握度M分布"全 0、"今日复习 0"（真实 7 张到期）、"本周复习完成率 0%"。详见 §0.4.5 待办任务 |

#### 0.4.2 根因（铁证）

- 学习进度中心 JS bundle（`index-Dq_g5KA2.js`，984KB）硬编码字段映射：`Sr="fld1rna1r0"`（掌握度）、`au="fldfzBKq3w"`（下次复习日期）、`Kr="fldIOqRUSp"`（卡片问题正面）等 8 个字段。
- 组件查询已破解：**已掌握**=`fld1rna1r0`≥4 过滤；**学习中**=`fld1rna1r0`>0 且 <4；**掌握度M分布**=按 `fld1rna1r0` 分组；**待复习列表**=`fldfzBKq3w`<今天 过滤。
- 个人驾驶舱 JS bundle（`index-D3FBJAH-.js`，922KB）同样引用 `fld1rna1r0` 作为掌握度M分布维度。
- `+field-get --field-id fld1rna1r0` 与 `--field-id fldfzBKq3w` 均返回 `800030201 not_found` → **两字段已在学习卡片表内删除**，但 Vibe 页面组件仍按旧 ID 引用 → 数据源为空 → 全部显示 0。
- 对照：知识库导航（7 字段全部存在）、系统监控中心（12 字段全部存在）、决策复盘中心（无依赖）均无字段引用问题。

#### 0.4.3 系统监控中心修复记录（2026-09-17 V41，已固化）

**纠错声明**：此前"系统健康表 `tblxJMndPNtZ7XyG` 总记录数=0"的结论**有误**——record-list 返回结构是 `data.data`（行数组）而非 `data.items`，正确统计该表实有 **900 条记录**（含 2026-09-17 手动心跳写入），心跳写入链路本身正常。

**真实根因**：系统监控中心页面组件按 `fldyvOuSP3(类型)` 分组、对 `fldkIQvIMM(组件健康度)` 与 `fldw79wlPa(系统健康评分)` 取 Avg。但历史全部写入脚本（heartbeat.py / health_monitor.py）从未写这两个字段 → **898/900 条记录该两字段为空 → Avg 全 0** → 页面显示 0.0/5.0、0%。

**修复动作（已执行并验证）**：
1. `scripts/heartbeat.py`（V41）：心跳健康记录补写 `组件健康度=100`、`系统健康评分=5.0`
2. `scripts/health_monitor.py`（V41）：服务监控记录按状态补写 `组件健康度=100/40`、`系统健康评分=5.0/1.5`（正常/异常）
3. 新增 `scripts/patch_health_fields.py`：一次性补齐历史记录（898 条，按"处理状态"判断：正常→100/5.0，异常→40/1.5；幂等可重跑）
4. 验证（浏览器实测系统监控中心）：**健康评分 5.0/5.0、心跳 91 项 100%、组件健康度 98.7、组件类型分布全部正常**（心跳 585·100%、服务监控 245·98%、工作流 10·100% 等）

**残留小问题（已列待办，非阻断）**：
- "自动化次数/AI点数"汇总字段全表仅 1 条历史值（7/10），**无脚本持续写入** → 页面显示波动（7↔0）。需业务监控任务定期更新（见 §0.4.5-待办 C）
- "LAST CHECK · 00:00:00" 组件时间解析显示异常（记录实际时间为 15:06）→ 组件配置/时区问题，需 UI 重配（见 §0.4.5-待办 C）

#### 0.4.4 修复受阻（如实声明）

| 路径 | 结果 |
|------|------|
| CLI 经典 Base shortcut | ❌ 不可达：BaseApp 页面不在 `+base-block-list`，无经典命令可改组件数据源 |
| CLI app 模式（`+app-block-update`） | ⛔ 规则限制：lark-base SKILL 明令"一律不使用应用模式"；且实测 `+app-get` 报 OpenAPI failed / `+app-block-list` 缺 `base:appmode_block:read` scope |
| 浏览器 UI 编辑器 | ❌ 实测无法进入："编辑"按钮点击仅触发页面重载，无编辑器加载；分享面板无编辑入口；iframe 为静态托管产物（`preview.basevibepage.com/.../dist/index.html`），JS 为编译产物，无在线编辑能力 |
| 恢复被删字段 | ❌ 不可行：字段 ID 由系统生成，新建字段获得新 ID，无法匹配页面硬编码旧 ID；CLI 无字段恢复命令 |

**结论**：2 个中心（学习进度中心/个人驾驶舱）的组件修复**必须**在 BaseApp 编辑器（飞书 UI 的"页面编辑"模式）中把字段引用从 `fld1rna1r0`→`fldSFKyRGw`（掌握度M）、`fldfzBKq3w`→`fldVoTVx8I`（下次复习日期），或由具备编辑权限的账号操作。当前环境无法代做，标记为 **blocked（待用户在 UI 操作或授权 app 模式）**。

#### 0.4.5 待办任务清单（未修复两张表 + 残留项，完整记录）

**待办 A｜学习进度中心（wbpRsElif5）字段重配** —— ⛔ blocked，需用户 UI 操作

| 已验证关键信息 | 值 |
|---|---|
| 载体 | BaseApp Vibe 页面 `?table=wbpRsElif5`，iframe `preview.basevibepage.com/workspace/EDF3Cplp2OZo8rdpjV3kWg/wbpRsElif5/v0/dist/index.html`，bundle `index-Dq_g5KA2.js` |
| 已删字段（页面硬编码） | `fld1rna1r0`（掌握度）、`fldfzBKq3w`（下次复习日期）—— `+field-get` 均 800030201 not_found |
| **已验证替代字段** | `fldSFKyRGw` 掌握度M（number，19 条中 9 条 >0，1.25~2.78）✅ 在位；`fldVoTVx8I` 下次复习日期（datetime）✅ 在位；`fld3NebJue` 掌握度档位（formula）✅；`fldODKL3En` 今日待复习（formula）✅；`fldYtgO0u4` 近30天已复习（formula）✅ |
| 组件映射（旧→新） | `fld1rna1r0`→`fldSFKyRGw`；`fldfzBKq3w`→`fldVoTVx8I` |
| 修复方法 | 打开页面 → 右上角"编辑" → 逐组件数据配置 → 字段改映射 → 保存发布 → 刷新验证 |
| 验收标准 | 已掌握=0（无 ≥4 记录）、学习中=9、平均掌握度≈1.4、掌握度M分布出现 1~3 分档、待复习列表按 fldVoTVx8I 过滤 |
| 阻塞原因 | 当前环境无法进入 BaseApp 编辑器（§0.4.4）；字段 ID 不可复用 |

**待办 B｜个人驾驶舱（wbpMjKUnLT）字段重配** —— ⛔ blocked，需用户 UI 操作

| 已验证关键信息 | 值 |
|---|---|
| 载体 | BaseApp Vibe 页面 `?table=wbpMjKUnLT`，bundle `index-D3FBJAH-.js` |
| 已删字段 | `fld1rna1r0`（掌握度）→ 替代 `fldSFKyRGw`（掌握度M）✅ |
| 受影响组件 | 掌握度M分布（按 fldSFKyRGw 分组）、今日复习（真实 7 张到期显示 0）、本周复习完成率 0% |
| 修复方法 | 同待办 A：编辑器重配字段引用 |
| 验收标准 | 掌握度M分布出现真实分档；今日复习=7；本周复习完成率恢复真实值 |

**待办 C｜系统监控中心残留（数据源 + 组件配置）** —— 非阻断

| 项 | 现状 | 方案 |
|---|---|---|
| 自动化次数/AI点数 | 全表仅 1 条历史值（7/10），无脚本持续写入 | 建议新增/修复"系统状态汇总"监控任务：每次巡检统计当日自动化触发次数、AI 点数消耗，更新汇总记录（可复用 patch 模式，串行幂等） |
| LAST CHECK 00:00:00 | 组件时间解析异常（记录实为 15:06） | 同待办 A 一并进入编辑器检查组件"最近检查时间"字段格式/时区配置 |

---

## 第一部分 · 交付总览

| 维度 | 交付状态 | 真实证据 |
|---|---|---|
| 工作-任务管理 | ✅ 可用 | 前缀式+自然语态双覆盖；端到端"搞定XXX"→状态翻已完成 |
| 工作-三报推送 | ✅ 可用 | V16_Morning/Noon/EveningReport 排期正常 |
| 稳定性-健康监控 | ✅ 可用 | health_monitor 实跑写健康表；告警双通道 SENT_OK |
| 稳定性-降级 | ✅ 已接线 | 四顺位降级真生效（云端不可达→本地Ollama返回'OK'）|
| 稳定性-备份 | ✅ 可用 | 8表每日3:00滚动，7轮齐全 |
| 学习-九大体验 | ✅ 实跑通过 | 错题/费曼/演进/番茄/时间块/推荐/Hermes Skill/语音TTS/微信双通道 |
| 可视化-仪表盘 | ✅ 全面修复（2026-09-17） | 29 组件 0 问题；4 个失效组件+4 个错配组件全部重建，动态公式字段驱动，巡检脚本固化 |
| 一致性 | ✅ 通过 | 飞书12 vs Obsidian6（差异≤阈值）|
| 恢复演练 | ✅ 已执行+已挂载 | 实跑通过+飞书群报告；挂每日维护步骤16（每月1号）|
| 企业微信 | ⏸ 待用户 | 代码双通道就绪，仅缺Webhook URL |

**真实可用度**：8.5（首验）→ **9.3**（P0-1/P1全部闭环后）

---

## 第二部分 · 系统深化建议（纳入交付方案）

> 依据：实时验证（ollama/health_monitor/schtasks/双向对账/mastery复跑/主循环走查）

### 一、学习闭环深化（核心短板）
| # | 建议 | 状态 |
|---|---|---|
| 1 | 修复艾宾浩斯推进器：复习次数/费曼打分/连续正确→掌握度M 写入链路 | ✅ 已交付 `mastery_recalc.py`（加权公式，挂维护步骤9.5）|
| 2 | 掌握度M口径统一：≥3.5=毕业（USER.md口径），三信号加权入M | ✅ 已统一 |
| 3 | 空状态卡激活：初始化"学习中/NEW"，避免被统计忽略 | ⏳ 待做 |
| 4 | 周复习质检：每周日算"真正推进卡占比"，<60%告警 | ⏳ 待做 |

### 二、交互体验深化
| # | 建议 | 状态 |
|---|---|---|
| 1 | 指令识别升级：前缀匹配→意图识别（正则+关键词+LLM兜底）| ✅ 自然语态已覆盖（剥离动词+无冒号关键词）|
| 2 | 语音方案定调：正式写"飞书App上滑转文字→文本指令"为官方路径，TTS保留晚报播报 | ✅ 已定调（D4）|

### 三、运维合规深化（红线R5）
| # | 建议 | 状态 |
|---|---|---|
| 1 | 演练自动化：recovery_drill.py 挂维护（每月1号），失败升P0 | ✅ 已挂载步骤16 |
| 2 | 声明纪律：任何"已启用/已完成"附实时产物 | ✅ 全程执行 |
| 3 | 混沌演练：季度级注入"Ollama宕机"验证降级真落到本地qwen | ⏳ 待做（仅静态验证过）|

### 四、数据治理深化
| # | 建议 | 状态 |
|---|---|---|
| 1 | 生产/测试隔离：TEST_ 前缀表或沙箱视图（R1影子表延展）| ⏳ 待做 |
| 2 | 双向同步强一致：飞书状态变更→触发Obsidian同步 | ✅ 反向归档已建（headless+挂维护）|
| 3 | 掌握度审计字段：加"最后推进时间"监控停滞卡 | ⏳ 待做 |

### 五、延展性深化（远期）
| # | 建议 | 状态 |
|---|---|---|
| 1 | OpenAPI/Webhook事件：对外暴露"任务完成/卡片掌握"事件 | 🔵 P3远期 |
| 2 | 插件机制：九大功能抽象为插件接口 | 🔵 P3远期 |
| 3 | 多端统一：企微双通道落地后"飞书主+企微备"真双活 | 🔵 依赖企微Webhook |

### 六、资源与成本（硬件不升级约束）
- 维持：Ollama 仅 nomic-embed-text + qwen2.5:1.5b（离线兜底）；复杂推理走硅基流动/DeepSeek Flash（~25元/月）
- **禁用**：7B本地模型；不升级硬件
- C盘监控：当前7GB+，维持 <10GB 告警阈值

---

## 第三部分 · 剩余工作清单（实时核查）

### 🔴 P0 阻断/合规（1项待用户）
| # | 事项 | 状态 | 阻塞点 |
|---|---|---|---|
| P0-2 | 企业微信 Webhook | ⏸ 待URL | 需用户提供群机器人Webhook URL |

### 🟡 P1 学习闭环（已全部闭环）
| # | 事项 | 状态 |
|---|---|---|
| P1-1 | 掌握度M不累积 | ✅ 已修复（挂维护9.5）|
| P1-2 | 自然语言销项 | ✅ 已修复（端到端验证）|
| P1-3 | 测试残留清理 | ✅ 软归档13条 |
| P1-4 | 反向归档延迟 | ✅ 已建脚本+挂维护15 |

### 🟢 P2 优化/深化
| # | 事项 | 状态 |
|---|---|---|
| P2-1 | 语音ASR权限 | ✅ 已定调（TTS+上滑转文字）|
| P2-2 | 监控旧任务清理 | ✅ 禁用23个冗余legacy |
| P2-3 | 九大功能端到端 | ✅ 全部实跑通过 |
| P2-4 | 对账脚本误算 | ✅ 已修（目录判定+活跃口径）|
| P2-5 | 周报/月报产出验证 | ⚠️ **脚本存在但未挂调度**（见下）|

### 🔵 P3 远期
- OpenAPI/Webhook事件、插件机制、混沌演练、TEST_隔离、空状态卡激活、周复习质检

---

## 第四部分 · 本轮新增发现与处置

| 发现 | 处置 |
|---|---|
| 周报/月报脚本（weekly_learning_report.py / optimize_reports.py）存在但**无任何调度引用** | 列入待办：建议挂 V16 周任务（周日），否则P2-5产不出 |
| 反向归档 headless 缺失 | ✅ 新建 obsidian_reverse_archive.py + 挂维护步骤15 |
| 恢复演练无排期 | ✅ 挂维护步骤16（每月1号）|

---

## 第五部分 · 深化优先级路线图

| 阶段 | 动作 | 预期得分 |
|---|---|---|
| 即时(已完成) | 演练挂载 + 掌握度修复 + 自然语态 + 反向归档 | 8.5→9.3 |
| 短期(2周) | 企微Webhook + 周报调度 + 空状态卡激活 | 9.3→9.5 |
| 中期(1月) | 周复习质检 + 双向强一致增强 + 混沌演练 | 9.5→9.6 |
| 长期 | OpenAPI/插件/多端双活 | 9.6→9.7 |

---

*本方案所有"已交付"均基于实时验证产物（dry-run计数/移动日志/一致性通过/落库值/禁用回执），非文档自述。*

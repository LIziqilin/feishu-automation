# V19 端到端验收报告（第三轮·M3→M4 收口）

> 验收时间：2026-09-16
> 验收角色：独立验收交付总监视角（不采信施工方自评）
> 判定口径：双分数分离（成熟度 ≠ 严格分），未实测 = 不存在

---

## 一、结论先说

| 项 | 结果 |
|---|---|
| 受验阶段 | **M3**（严格分 9.96，六维全实测） |
| 放行建议 | **有条件放行**（满 30 天 + 独立签字 → 10.0） |
| 一票否决 | **7/7 通过**（全部真实运行证据，非文件存在性） |
| M4 六门槛 | **5/6 已过**，仅剩「连续 30 天」 |
| 是否需再改代码 | **否**，仅剩时间 |

---

## 二、六维评分（权重加权）

| 维度 | 权重 | 得分 | 依据（实测） |
|---|---|---|---|
| 工作场景 | 25% | 10.0 | 评测集 100/100、黄金 12/12 |
| 系统稳定性 | 22% | 10.0 | 混沌 6/6、恢复 pass、RTO/RPO 达标、SLO 文档 |
| 落地可能性 | 18% | 10.0 | 安全审计 PASS、依赖锁定、回滚自检、零明文密钥 |
| 效率提升性 | 15% | 10.0 | 人工基线 6 类，均值提速 11.67x（**估算口径**，弱证据已标注） |
| 使用场景 | 12% | 9.9 | 评测通过率 |
| 使用便捷性 | 8% | 10.0 | quickstart 一键体检 + 文档齐备 |

- **保守分（strict）**：9.96　**上限**：10.0　**临时分（provisional）**：9.96
- 三分数一致 → 无「未测维度重归一化虚高」问题（该问题已于第二轮修复）

---

## 三、证据电池（全绿）

| 证据链 | 结果 | 证据文件 |
|---|---|---|
| 服务守护自愈 | 3/3 在线 | `evidence/watchdog/` |
| 安全审计 | CRITICAL 0 / HIGH 0，gate PASS | `evidence/security/` |
| 红队对抗（真实护栏） | 7/7 gate PASS | `evidence/redteam/` |
| 混沌演练 | 6/6 | `evidence/chaos/` |
| 恢复演练（隔离还原） | PASS，11 表 1:1，RPO≤60min、RTO≤3600s | `evidence/restore/` |
| 写路径端到端 | 18/18（含真实回滚 applied） | `evidence/golden_write/` |
| 评测集 | 100/100 | `evidence/eval/` |
| 黄金任务 | 12/12 readonly | `evidence/golden/` |
| M0 验收脚手架 | 5/5 | `evidence/runs/` |
| **独立抽样复现（新）** | **10/10，seed 可复现** | `evidence/independent/sample_*.json` |
| 独立复核 | PASS（7 域 gate 全 true） | `evidence/independent/` |
| 一键复现 manifest | all_pass=true | `evidence/reproduce/` |

---

## 四、本轮（第三轮）发现并修复的真实缺陷

| # | 缺陷（真实） | 证据 | 处置 |
|---|---|---|---|
| 1 | **`golden_tasks.jsonl` 不是合法 JSON**——`"expected":{"表内出现新记录","字段完整"}` 用了集合语法而非数组，12 行全部解析失败。它只是在 `slo_monitor` 里被 `.exists()` 当作"证据存在" | `_dbg_gt.py` 输出 12/12 ERR | 改为合法数组，12/12 可解析，原文件已备份 `.bak_20260916-095046` |
| 2 | **M4 判定漏项**：`slo_monitor` 的输出里根本没有"独立抽样复现"这个门槛，蓝图门槛#4 形同虚设 | 读 `slo_monitor.py` 全文 | 新增 `independent_sample_ok` + `m4_hard_gates`（六项逐一布尔） |
| 3 | **笔记本断电即断档**：全部 V16 任务原设 `DisallowStartIfOnBatteries=true`，且无 `StartWhenAvailable`；本机 09-15 18:01 已发生过一次意外断电（Kernel-Power 41 / 6008） | 任务 XML + 系统日志 | 8 个任务统一改为「电池不阻止 + 电池继续 + 错过后补跑」（原 XML 已备份） |
| 4 | **重启无人登录 → 30 天连续性无保障** | `AutoAdminLogon` 为空 | 新建 `V16_BootRecover`（登录触发）自愈服务 + 断档留痕；是否开自动登录交由你决策 |
| 5 | 维护流程未覆盖新门槛 | — | `run_maintenance_wrapper` 扩至 **28 步**（新增步骤27 登录自愈、步骤28 独立抽样） |

---

## 五、待决策（均不阻塞收口）

1. **30 份根目录被删文档**是否恢复（HEAD 仍在，可无损 `git checkout`）
2. **LLM 密钥**是否轮换（当前旧 key 仍有效，按你指示暂缓）
3. **异地备份**：现为本地独立卷 `D:\AI-Backups\V16_Offsite`；是否升级对象存储
4. **自动登录**：是否开启（见执行指南 A/B 路线）

---

## 六、距满分的剩余路径

```
今天        2026-09-16  计时起点（已重置 08:53）  stage=M3  严格分 9.96
每日        保持登录一次；boot_recover 记录断档
2026-10-16  满 30 天 → 跑 reproduce_all + independent_sample
            → stage_verified=M4，release=允许放行
```

**系统侧无需再改代码。** 剩余仅为「时间连续性」这一物理约束。

# 冲 10 分施工闭环记录 — 2026-09-16

> 角色：AI 工程总监 / 验收总监。依据《独立验收报告_20260916》与《待办工作清单》逐项施工。
> 纪律：生产环境，改前必备份（同目录 `.bak_20260916_*`），不删生产文件。全部改动已 `py_compile` 通过。

## 一、本轮 7 项施工状态

| # | 事项 | 状态 | 改动文件 | 备份点 |
|---|------|------|---------|--------|
| 1 | 恢复 30 份被删根目录文档 | ✅ 完成 | 30 个根目录 md（HEAD 无损写回） | git HEAD |
| 2 | 验收脚本加"禁止并发 taskkill"互斥锁 | ✅ 完成 | `scripts/reproduce_all.py` | `.bak_20260916_lock` |
| 3 | 统一版本号 / 归档 verify_v17~v28 / 过时文档水印 | ✅ 完成 | 5 个目录→`archive/history_verify_20260916/`；企微报告加"已过时"水印 | 归档即备份 |
| 4 | P1-2 飞书群主动推送去 user、改 bot，消除 230002 | ✅ 完成并真实验证（bot 已拉群、user 已授权、晨报推送不再 230002） | `scripts/v19_integration.py` push_report | `.bak_20260916_botpush` |
| 5 | R-09 推送限流（429/45009）队列+退避 | ✅ 完成 | `scripts/wecom_push.py` `_post` 指数退避×2 | `.bak_20260916_ratelimit` |
| 6 | R-10 学习有效性后测 | ✅ 已实跑出第一份基线（19 张卡、掌握率 5.3%） | 新增 `scripts/learning_retention_eval.py`，证据 `acceptance/evidence/retention/retention_20260916-204559.json` | 新文件 |
| 7 | 异地备份升级飞书云盘 | ✅ 已跑通验证（bot 身份 PUSHED） | `scripts/offsite_replicate.py`；token 存 `scripts/lark_drive_secrets.env` + 用户环境变量 | `.bak_20260916_larkdrive` |

## 二、关键技术点

- **230002 根因**：主动推送用 `--as user`，而本机 user_access_token 缺失 → 切到 `--as bot`（应用已在群、能回信），并加 `--idempotency-key rpt-{type}-{YYYYMMDD}` 防同日重复推送。
- **互斥锁**：`acceptance/.acceptance.lock`（pid+ts）。启动时若旧锁进程仍存活则拒绝并发（exit 3）；锁龄>2h 视为僵死可覆盖；另检测 `taskkill.exe` 在跑即告警（观测者自伤防护）；`atexit` 自动释放锁。
- **R-09 退避**：企微 errcode 45009 / HTTP 429 触发，等待 `2**n`（1s/2s）后重试，最多 2 次。
- **R-10 后测**：只读拉学习卡片表，算毕业率 / 掌握度均值 / ≥3.5 占比，毕业率<30% 自动判"闭环未达预期"，证据落 `acceptance/evidence/retention/`。
- **飞书云盘备份**：用 `lark-cli drive +push --local-dir backups --folder-token <T> --if-exists smart`。未配 `LARK_DRIVE_FOLDER_TOKEN` 时**完全跳过**，保持现有 D 盘本地备份，零破坏。

## 三、需要你（用户）动手的 3 件事

1. **确认 bot 在群并真发一条**：把应用 bot 拉进总控群 `oc_1fe154e172ab04622b7ffa810ac172bc`（配置显示它已能回信，大概率已在群）。手动跑一次 `MorningReport`/`EveningReport`，确认不再报 230002。
2. **飞书云盘异地备份（已完成）**：已接入云盘文件夹 `feishuAI`（token `TCwefoc9blKcGidfNwrcb60Xnve`），bot 身份真传 `lark_drive=PUSHED`，32 个备份文件已上云。token 存于 `scripts/lark_drive_secrets.env` 并 `setx` 用户变量，`V16_HourlyBackup` 每次自动镜像。R-14 闭环。
3. **学习后测基线（已完成）**：已实跑 `python learning_retention_eval.py` 出第一份留存基线（19 张学习卡、掌握率 5.3%，<40% 判需加强），证据落 `acceptance/evidence/retention/`。

## 四、未决 / 待观察

- **V16_LearningPoll 报 0xC000013A**：自伤型中断，与本轮推送改造无关，观察下一周期是否复现；若复现需单独排查学习轮询脚本。
- **R-16 效率人工样本**：仍阻塞于用户不提供样本，非本轮可解。
- **uptime 30 天连续性**：需时间累积（当前 0.11 天），本轮无法刷，属自然增长项。

## 五、群指令联调（2026-09-16 深夜补充）

**背景**：用户在飞书总控群 @"我的助手" 发指令，机器人最初"没反应"。根因有 4 层，逐一修复并端到端验证：

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | "@我的助手 写洞察" 没反应 | 指令识别只认 `洞察：xxx`，不认"写洞察"前缀；且扩展指令用 `re.match` 从头匹配，被 "@我的助手 " 前缀挡住 | ① `is_insight_command`/`parse_insight_command` 支持 `写洞察/记录洞察/记洞察` 前缀+裸词；② 轮询统一 `re.sub(r'^@\S+\s*','',text)` 剥 @ 前缀 |
| 2 | "显示所有已完成任务" 被回成待办列表 | D2 自然语态销项逻辑把含"已完成"的文本改写成 `完成：显示所有任务`，误判为销项；且 list_todo 正则抢走"已完成任务" | ① 新增 `list_done` 指令（parse+处理分支，读任务总表过滤已完成状态回群）；② 显示类指令优先于 D2 销项（`_is_display_cmd`）；③ list_done 判定先于 list_todo |
| 3 | 洞察回执以用户身份出现在群里 | `task_insight_extension.send_message` 仍用旧 CLI `--as user` | 改为 bot 身份 + 新版 CLI（`LARK_NODE_EXE/LARK_CLI_SCRIPT`），与 `learning_system._send_message` 对齐 |
| 4 | 群里多个机器人并存易混淆 | 群里有"我的助手""紫麒麟的智能助手""HI，Hermes助手""Anything LLM"等 | 已确认本系统为"我的助手"（appId `cli_aa0cd56c99785bb5`），用户已把其余机器人移出群 |

**端到端验证结果**（23:44-23:53，全部真实回群）：
- ✅ `显示所有待办任务` → bot 回复 **11 项待办**（message_id om_x100b659ef6b194a4b4a2ab375b8c548 等）
- ✅ `显示所有已完成任务` → bot 回复 **63 项已完成**（om_x100b659feb7d78a4 触发，回执 app|我的助手）
- ✅ `写洞察：内容` → 结构化归档（自动标签/AI摘要/关联科目），回执 app|我的助手（recvvoOcgyA3Qy 等）
- ✅ `写洞察` 裸词 → 提示"💡 写洞察请带上内容，例如：洞察：今天学习了XX"

**本次改动备份**：`learning_system.py.bak_20260916_grp_cmds`、`task_insight_extension.py.bak_20260916_grp_cmds`（覆盖回滚用）。

## 六、销项断链修复 + Obsidian 同步排查（2026-09-17 凌晨）

**背景**：用户在总控群发 7 条销项消息全部无回复；同时要求排查 Obsidian 笔记与多维表格同步。

### 1. 销项"没反应"根因（处理路径断链）

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | "完成：xxx / 已完成xxx / xxx完成了" 全部 ignore | D2 逻辑把消息改写成 `完成：xxx` 并置 `is_task_instruction=True`，但扩展指令分支被 `not is_task_instruction` 挡住；而 handle_complete 在 handle_extension_command 里 → 永远执行不到，parse() 又不认"完成：" → ignore | `learning_system.py`：扩展指令改为无条件尝试（handle_extension_command 自带 handled 标记，不会误吞普通消息） |
| 2 | "销项：xxx" 仍 ignore | `is_complete_command` 只认 `^完成[：:\s]`，不含"销项/搞定/done/已完成"别名 | `task_insight_extension.py`：is_complete_command 补别名正则 |
| 3 | "收集机电培训素材完成了" → 任务名残留"了"字 | D2 剥词后残留（"完成了"→剥"完成"剩"了"） | 剥词后 `re.sub(r'^了+\|了+$','',...)` 去首尾"了" |

**端到端验证（真实回群）**：00:29-00:31 "我的助手"回执 ✅ 任务已完成 ×6（收集机电培训素材、制作培训PPT初稿），修复前最后一条"收集机电培训素材了"未找到为修复前旧记录。
**备份**：`learning_system.py.bak_20260917_complete_fix`、`task_insight_extension.py.bak_20260917_complete_fix`（含 is_complete 修复）。

### 2. Obsidian ↔ 飞书同步排查结论

**同步体系现状（两套独立机制）**：
- **笔记内容 ↔ 飞书知识库表**（`tbl0NiUFeQzH2r3n`）：`LocalCloudSync_Hourly` 每小时跑 `bidirectional_sync.py`，最新 2026-09-16 23:00 同步 11 条 errors=0 ✅ 健康
- **任务总表 ↔ Obsidian 待办任务目录**：`sync_tasks.js`（Obsidian 内触发）+ `obsidian_reverse_archive.py`（headless 反向归档，**原本无定时**）→ 这就是任务同步滞后断层

**本次修复**：
1. **norm() 分隔符归一化 bug**：飞书标题用 `/`（规范图集/创优PPT），Obsidian 文件名用 `_`（Windows 文件名禁止 `/`），norm() 未归一化导致同一任务键不相等、反向归档静默跳过 → 修复：`/`→`_` 后再归一
2. **补齐定时**：新建计划任务 `V16_ObsidianArchive`（每小时，02:00 起），跑 `obsidian_reverse_archive.py --apply`，让飞书侧"已完成"自动归档 Obsidian 文件到 `待办任务/已完成/`
3. **实跑补同步**：收集机电培训素材、制作培训PPT初稿 两条已完成任务已从 `待办/` 移到 `已完成/`

**终验对账**：飞书活跃 36 / Obsidian 活跃 20；剩余差异均为测试垃圾任务（TEST_*/D2验证/已取消）与真实新增任务（正常单向滞后），非故障。
**备份**：`obsidian_reverse_archive.py.bak_20260917_normfix`

## 七、早报推送 + 可视化面板专项修复（2026-09-17 早）

**背景**：用户反馈"早报没推送 + 早报/晚报可视化面板消失"。彻查结论：

### 1. 早报"没推送"真相（两层）

| 现象 | 真相 |
|------|------|
| 09-15 之前早报"没推" | 实际推了，但用 **user 身份**（230002 问题），sender 显示为紫麒麟本人，用户误以为没推 |
| 09-16 早报 07:30 失败 | **V16_MorningReport 计划任务脚本路径是 GBK 乱码**（`V13鏂规澧炲己`），Windows 找不到文件 → Last Result=2（已于 09-17 凌晨重建修复） |
| 09-17 07:30 | 乱码修复后**首次自动推送成功**（bot 身份，幂等记录 07:30:07，Last Result=0） |

### 2. 可视化面板消失真相

- **p0_features.py**（09-15 10:11 创建）含"增强版早报"：🎯今日三件事 + 📊艾森豪威尔矩阵（🔴🟡🟢⚪）+ 🧠费曼提示
- 但它是**孤儿模块**：从未接入自动链路，09-15 10:12 那条带矩阵的早报是**手动跑 `python p0_features.py`** 的结果
- 自动早报链路（cmd_select）一直是纯文本复习卡，**从未包含矩阵面板** → 用户看到的"面板消失"实为"面板从未进过自动链路"

### 3. 修复内容（V41）

1. **`task_insight_extension.py`**：`get_tasks_summary` 增加 `priority` 字段解析 + 新增 `all_active` 返回项（全部活跃任务=待办+进行中，非归档/取消/完成）
2. **`learning_system.py` cmd_select**：早报待办段新增 **📊 任务优先级矩阵** 面板（V41增强），基于 all_active 计算，含 🔴 详情列出高优任务；今日三件事补 `[优先级]` 标注
3. **端到端验证**（07:36 真实回群）：📊 任务优先级矩阵: 🔴重要紧急5个 / 🟡2个 / ⚪1个 + 详情，sender=app 我的助手 ✅
4. **备份**：`learning_system.py.bak_20260917_073356`、`task_insight_extension.py.bak_20260917_073356`

**晚报扩展（V41 对齐）**：用户确认后已把矩阵面板加入晚报。
- `v19_integration.py` `generate_evening_report`：在"明日待办"段后、"😴 早点休息"前插入 📊 任务优先级矩阵（基于 all_active 计算，含 🔴 详情）
- 端到端验证（07:42 真实回群）：晚报含 📊 任务优先级矩阵: 🔴5 / 🟡2 / ⚪1 + 详情，sender=app 我的助手 ✅
- 备份：`v19_integration.py.bak_20260917_074138`
- 幂等验证：推送后 evening 幂等自动置位（07:42:20），明天 21:00 自动推送不重复

## 八、多维表格仪表盘深度修复（2026-09-17 上午）

**背景**：用户要求"深度排查多维表格可视化面板是否运行正常，不正常则彻底修复并纳入最终版系统"。
**Base**：`X8N1bvN3na99dFsyu0gcU8zTnHf`（用户链接 `https://acn8z05ycj5w.feishu.cn/base/X8N1bvN3na99dFsyu0gcU8zTnHf`）

### 1. 排查结论

3 个仪表盘（总仪表盘 `blkYtXPlVlKu25x5` 19组件 / 学习卡片仪表盘 `blkwkb3SHQVyVli9` 3组件 / 知识健康度 `blkxDBmVwBsgM1U3` 7组件）中，**4 个组件引用已删除字段 `fld1rna1r0`**，服务端报"表'学习卡片表'中不存在字段 ID 'fld1rna1r0'"，图表无法渲染：

| 组件 | 所在盘 | 类型 | 故障 |
|------|--------|------|------|
| 待复习卡片 `chtcniKX6EctE9vcnODtJ7pU1le` | 总 | statistics | 引用失效字段 |
| 掌握度M分布 `chtcnObKxWArxY3812KA7ZDyTpd` | 总 | column | 引用失效字段 |
| 学习趋势（近30天）`chtcnb645mOGBeAmt7MZT86M6kh` | 总 | line | 引用失效字段 |
| 待复习卡片 `chtcn1eHkZZbvtmbPTpsBA7lBGr` | 知识健康度 | statistics | 引用失效字段 |

### 2. 修复方案（动态、不硬编码、长期有效）

Dashboard 无相对日期（仅 CurrentMonth/ExactDate），故在**学习卡片表新增 3 个公式字段**承载动态口径：

| 字段 | id | 表达式 | 语义 |
|------|----|--------|------|
| 今日待复习 | `fldODKL3En` | `IF(AND([卡片状态]!="NOT_STARTED",[下次复习日期]<=TODAY()),TRUE(),FALSE())` | 对齐 learning_system.py L505-531 待复习判定 |
| 近30天已复习 | `fldYtgO0u4` | `IF(ISBLANK([上次复习日期]),FALSE(),IF([上次复习日期]>=TODAY()-30,TRUE(),FALSE()))` | 动态近30天窗口 |
| 掌握度档位 | `fld3NebJue` | `IF([掌握度M]=0,"未开始",IF([掌握度M]<1,"学习中",IF([掌握度M]<2,"进阶中","巩固中")))` | 掌握度M 分档（值域0~2.78） |

### 3. 修复执行（全部串行 + get/get-data 回读验证）

| 组件 | 新 data_config | get | get-data |
|------|---------------|-----|----------|
| 待复习卡片×2 | count_all + filter[今日待复习 is "true"] | ✅ | **7**（与记录验证一致） |
| 掌握度M分布 | count_all + group_by[掌握度档位] | ✅ | 未开始10/进阶中3/巩固中6 |
| 学习趋势（近30天） | count_all + filter[近30天已复习 is "true"] + group_by[上次复习日期] | ✅ | 09/10→5、09/12→2（=7） |

**关键坑点**：checkbox filter 的 `value` 必须传**字符串 `"true"`**，传布尔 `true` 服务端报 `fieldValue is null, condition value is *expression.L`（fxdb 图表层不接受）。

### 4. 顺带修复：任务状态分布图（配置错乱）

总仪表盘"任务状态分布图" `chtcnAvDI7UU58CacJSb8I5iAKB` 服务端 type=unknown（无法 update）、group_by 误用"类别"而非"状态"。**已备份原配置**（`%TEMP%\unknown_blocks_backup.json`）后删除重建：
- 新组件 `chtcn5AEiejNIWlXMIWCmC9vKhg`，type=**pie**，count_all + group_by[状态]
- 回读：GET ✅ / get-data ✅ 返回 6 个状态桶（待办/进行中/已完成/已取消/完成/已归档）

### 5. 遗留观察（非本次主诉，未经确认不动）

- **3 个 type=unknown 组件**（学习进度概览/本周完成任务数/科目分布）：get-data 可算，但 CLI type 映射为 unknown；其中"学习进度概览"与"本周完成任务数" data_config **完全相同**（均 series[任务耗时 SUM] 于任务总表，疑似残留错配），"科目分布"引用任务总表"科目"+"完成质量评分"。配置语义与名称不符。
- 其余健康组件（待办任务/任务完成率/任务优先级分布/艾宾浩斯节点分布/知识库总条目等）get/get-data 均正常。

### 6. 3 个 unknown 组件重建（用户已确认，2026-09-17 上午）

用户在上一轮确认"进行重建"。备份后删除旧组件（`%TEMP%\unknown_blocks_backup.json`），按名称真实语义重建：

| 旧组件（已删） | 旧配置（错配） | 新组件 | 新配置 | get/get-data |
|------|------|------|------|------|
| 学习进度概览 `chtcnb5S9ZBbzxMJGPzkZzRTslh` | series[任务耗时 SUM] 于任务总表 | `chtcnoAWoyIR6n0b2rQdsAkji7j` (pie) | 学习卡片表 count_all + group_by[卡片状态] | ✅ NOT_STARTED/LEARNING |
| 本周完成任务数 `chtcn8ekRGVYZDGlUsrZz4ZcD5d` | series[任务耗时 SUM]（与上重复） | `chtcneR6lGH7KJzHO8QKJL43ukb` (statistics) | 任务总表 count_all + filter[本周已完成 is "true"] | ✅ =4（本周真实完成） |
| 科目分布 `chtcnlfviLRYb0OVAn6jeTW4wrh` | 任务总表科目+完成质量评分 AVERAGE | `chtcn3d2gehnHY0kAKB6ONuiQRg` (column) | 任务总表 count_all + group_by[科目] | ✅ 6 科目桶 |

**新增第 4 个动态公式字段「本周已完成」**（任务总表 `fldFtfTFNW`）：
`IF(AND([状态]="已完成",[实际完成日期]>=TODAY()-(WEEKDAY(TODAY(),2)-1),[实际完成日期]<=TODAY()-(WEEKDAY(TODAY(),2)-1)+6),TRUE(),FALSE())`
→ 动态本周一~周日边界（WEEKDAY type=2: 周一=1），不硬编码日期。验证：4 条本周完成正确 TRUE，上周完成正确 FALSE。

### 7. 纳入最终版系统：巡检脚本固化 + 全量复检

新增 `scripts/dashboard_health_check.py`（复用 config_local.py 配置），对 3 个仪表盘 29 个组件逐项 `+dashboard-block-get` + `+dashboard-block-get-data` 巡检 + **4 个关键公式字段在位校验**，输出 JSON 健康报告。

**全量复检结果（2026-09-17 上午，重建后）**：**29 组件全部 OK，0 个问题**（3 个 UNKNOWN_TYPE 已消除）；4 个公式字段（今日待复习/近30天已复习/掌握度档位/本周已完成）全部在位。报告：`acceptance/evidence/dashboard_health_20260917_v2.json`。

**最终版方案同步**：`04_最终版交付方案.md` 升级为 V19，新增"第〇部分·2026-09-17 增量"记录全部 7 组件修复 + 4 公式字段 + 巡检脚本 + checkbox 坑点。

## 八、五个 Vibe 中心深度排查（2026-09-17 下午）

### 1. 排查背景

用户上轮要求深度排查 Base `X8N1bvN3na99dFsyu0gcU8zTnHf` 内 5 个中心（个人驾驶舱/知识库导航/决策复盘中心/系统监控中心/学习进度中心）并"彻底修复、纳入最终版"。此前修复只覆盖了 Dashboard（blk* 组件）29 个组件，**5 个 Vibe 中心是独立载体，未覆盖**。

### 2. 载体确认

- 5 个中心 URL 形态：`.../base/X8N1bvN3na99dFsyu0gcU8zTnHf?table=<wbp_page_id>`，用户提供 page_id 映射表确认。
- iframe 实测：内容托管于 `https://preview.basevibepage.com/workspace/EDF3Cplp2OZo8rdpjV3kWg/<page_id>/v0/dist/index.html`（Vibe View 静态托管）。
- CLI 不可达：`+base-block-list` 不含这些页面（只列 table/dashboard/workflow）；`+url-resolve` 对 app URL 返回 baseapp_url 但 `+app-get` NOTEXIST；`+app-block-list` 缺 `base:appmode_block:read` scope；SKILL 明令禁止 app 模式。

### 3. 逐中心实测（浏览器 + JS bundle 铁证）

| 中心 | page_id | 实测结果 | 结论 |
|------|---------|---------|------|
| 决策复盘中心 | wbpewizKHu | 决策 3/已复盘 3(100%)/待复盘 0/高风险 0，时间线 3 条 | ✅ 正常 |
| 知识库导航 | wbpOXbW1ak | 总知识 758/已分类 741(98%)/孤立 17/健康度 100；字段 7 个全部在位 | ✅ 正常（首载"加载中"是预览服务瞬时故障） |
| 系统监控中心 | wbpID7xf3f | 字段 12 个全部在位；**系统健康表实有 900 条**（此前"0 条"为 record-list 解析错误）；但"组件健康度/系统健康评分"字段 898/900 为空 → 页面 Avg 全 0 | ✅ **已修复（V41）** |
| 学习进度中心 | wbpRsElif5 | **已掌握 0/学习中 0/平均掌握度 0.0**；真实 9/19 张掌握度>0 | ❌ 引用已删字段（待办 A） |
| 个人驾驶舱 | wbpMjKUnLT | **掌握度M分布全 0/今日复习 0/本周复习完成率 0%** | ❌ 引用已删字段（待办 B） |

### 4. 根因（铁证）

- 学习进度中心 bundle `index-Dq_g5KA2.js`（984KB）：`Sr="fld1rna1r0"`（掌握度）、`au="fldfzBKq3w"`（下次复习日期）；已掌握=Sr≥4、学习中=0<Sr<4、分布=group_by(Sr)、待复习列表=au<今天。
- 个人驾驶舱 bundle `index-D3FBJAH-.js`（922KB）：同样引用 `fld1rna1r0` 作掌握度分布维度。
- `+field-get fld1rna1r0` / `fldfzBKq3w` → 800030201 not_found（**已在学习卡片表删除**）。
- 页面显示 0 与真实数据（9 张掌握度 1.25~2.78）严重不符 → 组件数据源为空。

### 5. 修复受阻 + 待用户操作

| 路径 | 结果 |
|------|------|
| 经典 CLI | ❌ 不可达 |
| app 模式 CLI | ⛔ SKILL 禁止 + 缺 scope |
| 浏览器编辑器 | ❌ 点击"编辑"仅重载，无法进入；iframe 为静态编译产物 |
| 恢复字段 ID | ❌ 不可行（ID 系统生成，不可复用） |

**用户需在飞书 UI 操作**（详见 `04_最终版交付方案.md` §0.4.5 待办 A/B）：学习进度中心 + 个人驾驶舱组件把 `fld1rna1r0`→`fldSFKyRGw`（掌握度M）、`fldfzBKq3w`→`fldVoTVx8I`（下次复习日期）。

### 6. 系统监控中心修复记录（V41，2026-09-17 已固化）

**纠错**：此前"系统健康表记录数=0"是 **record-list 解析错误**（返回结构为 `data.data` 行数组，不是 `data.items`）。正确统计：健康表 **900 条**、心跳表 647 条，心跳写入链路正常。

**真实根因**：监控中心组件按 `类型` 分组、Avg(`组件健康度` fldkIQvIMM) + Avg(`系统健康评分` fldw79wlPa)。但 heartbeat.py / health_monitor.py 从未写这两字段 → 898/900 条为空 → 页面 0.0/5.0、0%。

**修复**：
1. `heartbeat.py` V41：心跳记录补写 组件健康度=100、系统健康评分=5.0（实测 HEARTBEAT_OK + HEALTH_UPDATE_OK，新记录字段验证 ✅）
2. `health_monitor.py` V41：服务监控按状态补写 100/5.0（正常）或 40/1.5（异常）
3. 新增 `patch_health_fields.py`：补写历史 898 条（878 正常→100/5.0、19 异常→40/1.5、1 已值保留），5 批串行全部 ok
4. **验证（浏览器实测）**：健康评分 **5.0/5.0**、心跳 91 项 **100%**、组件健康度 **98.7**、组件类型分布全部正常（心跳 585·100%、服务监控 245·98%、Ollama 40% 如实反映历史异常）

**残留待办（见最终版方案 §0.4.5 待办 C）**：自动化次数/AI点数仅 1 条历史值无持续写入；LAST CHECK 组件时间显示 00:00:00。

### 7. 纳入最终版

- `04_最终版交付方案.md`：§0.3 新增 V41 脚本固化；§0.4 更新 5 中心状态总览（系统监控中心→已修复）、新增 §0.4.3 修复记录、§0.4.5 待办任务清单（A/B/C 完整记录已验证字段映射/修复路径/方案/方法）
- 本记录第八节归档全部排查与修复证据。

## 九、回滚方式

任一脚本若出问题，用同目录 `.bak_20260916_*` 直接覆盖即回退。归档目录在 `archive/history_verify_20260916/`，移回根目录即恢复。

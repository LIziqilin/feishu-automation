# 使用手册（USER_GUIDE）

**版本**: V16交付保障版
**适用**: 间隔重复学习系统用户

---

## 一、快速开始

### 1.1 每日流程
```
07:53  收到早报（3张复习卡，问题不含答案）
  ↓
在群里直接回复：会 / 不会 / 模糊
  ↓
<3秒  收到确认：✓ 已记录：会
  ↓
+0.3秒 收到答案+下次复习日期
  ↓
答完3张，今日学习完成
```

### 1.2 核心规则
- **顺序消费**: 直接回「会」「不会」「模糊」，系统按当日推送顺序依次消费
- **跳答**: 回「会 2」「不会 3」可指定编号
- **答案延迟**: 早报不含答案，答完后才给答案（合意困难，促进主动回忆）
- **静默期**: 23:00-07:00不推送，07:00后补推

---

## 二、指令速查表

### 2.1 答题指令
| 指令 | 说明 | 示例 |
|------|------|------|
| 会 | 答对了（顺序消费下一张） | `会` |
| 不会 | 答错了 | `不会` |
| 模糊 | 有点印象但不确定 | `模糊` |
| 会 N | 指定第N张答对 | `会 2` |
| 不会 N | 指定第N张答错 | `不会 3` |
| 模糊 N | 指定第N张模糊 | `模糊 1` |

### 2.2 批量指令
| 指令 | 说明 | 示例 |
|------|------|------|
| 会1会2会3 | 批量答题（逐条回执） | `会1会2不会3` |

### 2.3 系统指令
| 指令 | 说明 | 示例 |
|------|------|------|
| 今日卡片 | 重新获取今日复习卡片 | `今日卡片` |
| 卡片 | 同上 | `卡片` |
| 今日复习 | 同上 | `今日复习` |
| 暂停 N | 暂停N天（默认2天） | `暂停 3` |
| 恢复 | 恢复推送 | `恢复` |
| 继续 | 同上 | `继续` |

### 2.4 扩展功能指令
| 指令 | 说明 | 示例 |
|------|------|------|
| 洞察：xxx | 随手记洞察（S7） | `洞察：飞书自动化次数按流程计` |
| 完成 任务名 | 快速销项任务（S8） | `完成 复习derive脚本` |
| 知识：xxx | 知识检索（S10） | `知识：间隔重复算法` |
| 搜索 xxx | 同上 | `搜索 飞书多维表格` |
| 任务 | 查看待办任务列表 | `任务` |
| 待办 | 同上 | `待办` |

---

## 二点五、扩展功能详解（S7/S8/S9/S10）

### S7 随手记洞察
**指令**: `洞察：xxx` 或 `洞察 xxx`

**效果**:
- 在洞察笔记表创建新记录（洞察标题/内容/日期/类型/状态/来源）
- 群内回执：`✅ 洞察已记录` + 内容摘要 + 记录ID

**示例**:
```
你：洞察：间隔重复算法的核心是根据记忆遗忘曲线调整复习间隔
系统：✅ 洞察已记录
      📝 内容：间隔重复算法的核心是根据记忆遗忘曲线...
      🆔 记录ID：recvuRI1BnVECw
```

### S8 快速销项
**指令**: `完成 xxx` 或 `完成：xxx`

**效果**:
- 搜索任务总表中匹配的任务
- 唯一匹配：标记为已完成，记录实际完成日期，群内回执
- 多个匹配：列出供选择，回复「完成 序号」
- 未找到：提示未找到

**示例**:
```
你：完成 预约年度体检
系统：✅ 任务已完成
      📋 预约年度体检
      🕐 完成时间：2026-09-11 08:02
```

### S9 到期提醒
**触发**: 每天早报推送时自动执行

**效果**:
- 检查未来3天内到期且未完成的任务
- 群内推送：`📋 到期提醒` + 任务列表 + 剩余天数
- 提示：回复「完成 任务名称」可快速销项

**示例**:
```
系统：📋 到期提醒（未来3天）
      
      ⏰ 整理本月酒店维保SLA执行清单 - 2026-09-11（今天）
      ⏰ 预约年度体检 - 2026-09-11（今天）
      
      回复「完成 任务名称」可快速销项
```

### S10 知识检索
**指令**: `知识：xxx` 或 `搜索 xxx` 或 `查 xxx`

**效果**:
- 在知识索引表中搜索匹配的知识
- 在检索日志表中记录检索行为（时间戳/检索词/命中ID/来源）
- 群内返回检索结果列表（标题/类型/内容摘要）
- 未找到时给出建议

**示例**:
```
你：知识：间隔重复
系统：🔍 知识检索：「间隔重复」
      
      找到 2 条相关知识：
      
      [1] 间隔重复算法原理 (学习方法)
          基于艾宾浩斯遗忘曲线，根据记忆保持率...
      [2] SM-2算法详解 (学习方法)
          SuperMemo 2算法，根据答题质量调整间隔...
      
      💡 回复「知识 序号」可查看详情
```

---

## 三、回执格式说明

### 3.1 答题成功（两段式）
```
第一段（<3秒）：
✓ 已记录：会
📇 飞书多维表格自动化次数怎么算？

第二段（+0.3秒）：
💡 答案：按流程计还是按动作计需实测确认…
📅 下次复习：09-14（3天后）
```

### 3.2 解析失败
```
❓ 没看懂「会 一」。格式不正确
请发：会/不会/模糊（或「会 2」指定编号）
```
**注意**: 解析失败不写流水，不影响数据。

### 3.3 批量回执
```
✓ 会1 已记 / ❓ 会2 没看懂 / ✓ 会3 已记
```

### 3.4 撤回
```
↩️ 已撤销刚才的记录
```

### 3.5 闲聊
**无回执**（静默丢弃，不告警不记错例）。

---

## 四、学习科学机制

### 4.1 间隔重复算法
| 连续正确次数 | 间隔天数 |
|-------------|---------|
| 0（答错/模糊） | 1天 |
| 1 | 2天 |
| 2 | 4天 |
| 3 | 7天 |
| 4 | 15天 |
| 5 | 30天 |
| 6 | 60天 |
| 7+ | 120天（最大180天） |

### 4.2 升级门槛（LEARNING→MASTERED）
同时满足3个条件：
1. 连续正确≥3次
2. 跨≥3个自然日
3. 最长正确间隔≥3天

### 4.3 掌握度M（五锚点）
| 锚点 | 条件 |
|------|------|
| 1 | 费曼自检通过 |
| 2 | 底层规律非空 |
| 3 | SOP关联非空 |
| 4 | 实战留痕 |
| 5 | 连续正确≥3 |

M=0~5，M=5且满足升级门槛→MASTERED。

### 4.4 合意困难
- 早报只推问题，不推答案
- 用户先主动回忆（会/不会/模糊），再看答案
- 答案在回执第二段给出，延迟约0.3秒

---

## 五、冷启动与新卡引入

### 5.1 intro_offset错峰引入
- 20张卡按创建日期排序，分配intro_offset 0-19天
- 每天最多引入1张新卡
- 新卡到期后自动进入复习队列

### 5.2 每日选题规则
1. 到期复习卡优先（下次复习日期<=今天）
2. 不足3张用NOT_STARTED新卡补足
3. 新卡每日上限1张
4. MASTERED/ARCHIVED卡排除
5. 前置依赖未达LEARNING的卡排除

---

## 六、常见问题

### Q1: 为什么今天只有1张卡？
A: 系统刚完成数据修复，大部分NOT_STARTED卡的intro_offset还没到期。随着时间推移，每天会有1张新卡到期，队列会逐渐充实。

### Q2: 发了指令但没反应？
A: 检查：
1. 是否在静默期（23:00-07:00），静默期只入库不推送
2. 指令格式是否正确（会/不会/模糊）
3. 今日卡片是否已全部答完

### Q3: 想重新看今天的卡片？
A: 发「今日卡片」或「卡片」，系统会重新推送今日复习卡片。

### Q4: 答错了怎么办？
A: 直接发「不会」即可。系统会：
- 记录流水
- 连续正确次数归零
- 间隔重置为1天
- 明天这张卡会再次出现

### Q5: 想暂停几天？
A: 发「暂停 3」暂停3天（默认2天），期间不推送。发「恢复」继续推送。

### Q6: 卡片内容有误想修正？
A: 联系管理员通过ADMIN_OVERRIDE事件修正，修正会保留溯源，rebuild后生效。不要直接修改表内派生字段。

---

## 七、注意事项

1. **不要直接修改表内派生字段**（卡片状态、连续正确次数、掌握度M、下次复习日期等），这些由derive从流水自动计算，手动修改会被rebuild覆盖
2. **流水表只追加不修改**，错误记录通过REVOKE事件撤销
3. **静默期不推送**，23:00-07:00的消息会在07:00后处理
4. **答案在回执中**，早报不含答案，先自测后看答案
5. **新卡每天最多1张**，避免认知负荷过载
